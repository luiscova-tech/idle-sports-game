# CLAUDE.md — Idle Sports Franchise Builder

This file is read automatically at the start of every Claude Code session in this repo. Keep it current — see "Maintenance" at the bottom.

## Project Summary
A multi-sport idle/incremental franchise management game. Browser-first (React + Vite), with a planned later wrap for iOS via Capacitor. Full design brief: see `idle-sports-game-brief.md` in repo root.

## Tech Stack
- React + Vite (TypeScript template)
- Zustand for game state (single central store)
- react-router-dom for routing
- Client-side persistence only for v1 (localStorage/IndexedDB) — no backend
- 2D flat icon/sprite art for v1 (AI-generated, style-locked prompts)

## Architecture Principles
- **One shared engine, not per-sport systems.** Core engine (match sim, economy, player model, facilities, season/standings, prestige, events) is sport-agnostic. Sport-specific rules (stat weights, scoring, roster size) live in plugged-in sport modules.
- **Currency types are separated in the data model from day one** (even premium/future currency fields), so monetization can be added later without a rewrite.
- **v1 ships with exactly 2 sports.** Do not add a 3rd sport until the shared engine has been validated with 2 working end-to-end.
- Keep save/economy logic isolated in its own module — not scattered across UI components.

### Engine / sport-module split (established in step 2, validated with a second sport in the "Baseball" amendment below)
- `src/engine/types.ts` defines the sport-agnostic contract: `MatchOutcome` ('win'/'draw'/'loss'), `TickResult<TState>`, `MatchResult<TState>`, an optional `MatchContext` (`{level?, opponentLevelRange?, matchLength?}` — see "Match/economy escalation", "Continuous win-probability model", and "Baseball" below) and the `SportModule<TState>` interface (`id`, `ticksPerMatch`, `createInitialState()`, `tick(state, tickIndex, context?)`, `getOutcome(state, context?)`, `getPerformanceFactor(state, context?)`, optional `isMatchComplete?(state)` — see "Baseball" for why a variable-length match needs this). `TState` is fully opaque to the engine — sport modules own their own state shape. `context` is optional everywhere it appears — a sport module free of any win-gating concept simply never reads it.
- `src/engine/tickEngine.ts` holds three pure, timer-free helper functions (`advanceTick`, `isMatchComplete`, `finalizeMatch`) that operate generically over any `SportModule<TState>`. It must never import from `src/sports/**` or reference sport-specific vocabulary. `advanceTick`/`finalizeMatch` both take an optional `context` and thread it straight through to whichever `SportModule` methods accept it — this file never inspects or interprets it. `isMatchComplete(module, tickIndexAfterTick, state)` consults `module.isMatchComplete(state)` when the sport module defines one (a variable-length match), else falls back to the original `tickIndexAfterTick >= module.ticksPerMatch` check (a fixed-length match, e.g. soccer) — the single shared authoritative "is this match over" check, used by both the store's real completion logic and the UI's display-timing checks.
- `src/engine/economy.ts` is the single isolated home for currency math — a `REVENUE_BY_OUTCOME` table keyed only on the generic `MatchOutcome`, shared by every sport rather than redefined per sport. `calculateMatchRevenue(outcome, performanceFactor)` additionally scales revenue by the sport module's generic 0-1 `performanceFactor` (see "Match/economy escalation" below) — this file only ever consumes that single generic number, never sport-specific stats. Also holds `expectedMatchRevenue(probabilities)` (see the seventeenth amendment) — already sport-agnostic since before baseball existed.
- `src/engine/winProbability.ts` (new in the "Baseball" amendment) is the single isolated home for the level-gap-driven win/draw/loss probability model — hoisted out of `soccerModule.ts` once it became clear a second sport needed the exact same math. Sport-agnostic: takes only two numeric levels, never sport-specific state.
- `src/engine/ventureTiers.ts` (new in the "Baseball" amendment) is the single isolated home for the Adventure-Capitalist-style tier-cost/revenue/pacing formulas (`VentureTierConfig`, `VentureTierState<TState>`, `tierUpgradeCost`, `trainingEffectMultiplier`/`TRAINING_MILESTONE_LEVELS`, `tierPerTickRevenue`, `opponentLevelRangeForTier`, `autoTickIntervalMsForTier`) plus the shared `resolveVentureTierTick<TState>()` per-tick resolution helper — also hoisted out of `soccerModule.ts` for the same reason.
- `src/engine/prestige.ts` is the single isolated home for prestige/Legacy Points math (see "Prestige system" below) — sport-agnostic, takes only a scalar "total earnings," never imports from `src/sports/**`.
- `src/engine/achievements.ts` is the single isolated home for the achievement/badge framework (see "Achievements" below) — sport-agnostic, consumes only a generic `Record<string, number>` stats record, never imports from `src/sports/**`.
- A sport plugs in by implementing `SportModule<TState>` in its own folder under `src/sports/<sport>/` (see `src/sports/soccer/soccerModule.ts` and `src/sports/baseball/baseballModule.ts`) — that's the only place sport-specific vocabulary (goals/possession/shots for soccer; innings/outs/at-bats for baseball) is allowed to appear.
- The idle loop's `setInterval`s live in exactly one place — a React hook (`src/hooks/useMatchTicker.ts`). The engine, economy, and store stay fully timer-free and synchronously testable. As of the "Match/economy escalation" amendment below, the hook runs one independent interval PER auto-playing tier (real-world pacing is now a per-tier concern, via `autoTickIntervalMsForTier()`) rather than a single shared `intervalMs` parameter — see that section for why. As of "Baseball" below, this generalizes across every plugged-in sport via namespaced interval-map keys (`"soccer:tierId"`/`"baseball:tierId"`).
- ~~The Zustand store (`src/store/useGameStore.ts`) instantiates one module-scoped sport module (currently `createSoccerModule()`), reused identically by every venture tier (see below) — never forked or reconfigured per tier.~~ **Superseded** — see "Baseball" below: the store now instantiates one module-scoped instance PER plugged-in sport (`soccerModule`, `baseballModule`), each reused identically by every tier of ITS OWN sport, with a parallel tier array (`tiers`/`baseballTiers`) and parallel store actions per sport.
- `src/components/VentureCard.tsx` is fully generic over `SportModule<TState>` as of "Baseball" below — it takes a sport module instance, tier config, and tier state as props, with zero hardcoded soccer imports. Each sport gets a thin adapter component (`SoccerVentureCard.tsx`, `BaseballVentureCard.tsx`) that wires up its own store selectors/actions and passes them down — this is the actual validation of "a second sport plugs in with zero engine changes," per this build-order step's own stated purpose.

### Manual-before-automated pattern (amendment to step 2)
Classic idle games hook players with manual interaction before automation is earned. Applied to soccer, and intended as the template for every future sport:
- A single per-tier resolution path (`tickTier(tierId)` in the store) advances that tier's match by one tick — it is called identically by the idle interval and by a manual player tap, so there is never a forked "manual" vs. "automatic" simulation branch.
- Each tier has its own `managerHired` boolean (default `false`) gating its own automation: `useMatchTicker` only auto-ticks tiers where `unlocked && managerHired` are both true. Before that, a tier only advances via its own manual "Push the Attack" button.
- `managerHired` is flipped by a one-time per-tier Revenue purchase (`hireManagerForTier(tierId)`, cost from that tier's config). This cost/unlock logic lives in the store/sport-module layer, not in `src/engine/economy.ts` — it's an idle mechanic ("pay currency to unlock automation"), not part of the sport-agnostic match-outcome economy.
- Chosen design: the manual action button stays visible and usable even after automation unlocks (a supplemental boost), rather than being hidden — matches the genre convention that active play keeps some value after idle progression is unlocked.
- Future sports should follow the same shape: a manual per-tick trigger wired to the same tick-resolution path, gated automation behind a purchasable unlock, no changes to `src/engine/**`.

### Venture tiers (second amendment to step 2 — Adventure-Capitalist-style restructuring)
The single global match view was replaced with a vertical stack of independent "venture tier" cards (`Local Game → Local Tournament → Regional Championship → National League → Continental Cup → World Championship → Legends' Circuit → Galactic League → Mythic Ascension → Eternal Championship → The Multiverse Cup` for soccer — tiers 5-6 added in the eighth amendment, tiers 7-11 added in the ninth amendment, the latter hidden until a player's first prestige), each its own parallel revenue generator:
- **Reuse vs. divergence:** every tier runs its match through the exact same `createSoccerModule()` instance and the exact same `advanceTick`/`isMatchComplete`/`finalizeMatch` (`src/engine/tickEngine.ts`) and `calculateMatchRevenue` (`src/engine/economy.ts`) — none of those files changed. The only per-tier divergence is a revenue multiplier (`baseRevenueMultiplier × level`) applied in the store, in `tickTier()`, on top of `economy.ts`'s base win/draw/loss payout. `economy.ts` itself has no concept of tiers.
- Tier names/multipliers/costs are sport-specific vocabulary, so they live in `src/sports/soccer/soccerModule.ts` (`SOCCER_VENTURE_TIERS`, `tierUpgradeCost()`) — the same file that owns "goal"/"chance"/etc. A second sport (step 3) defines its own tier list the same way.
- **Store shape:** `useGameStore.ts` now holds `tiers: VentureTier[]` (id, unlocked, level, managerHired, tickIndex, match, matchesCompleted, cumulativeRevenue, lastOutcome) instead of one global match object, plus the store-level actions `tickTier`, `upgradeTier` ("Improve Training" — raises `level`, cost grows per level via a mild exponential curve), and `hireManagerForTier`.
- ~~**Unlock-threshold logic chosen:** a locked tier unlocks once the immediately preceding tier's `cumulativeRevenue`... reaches a configured threshold, auto-checked every tick.~~ **Superseded** — see "Tier unlocking as a player purchase" below; unlocking is now a deliberate spend from the current Revenue balance, not an automatic threshold.
- Revenue stays ONE global currency (`currencies.revenue`), spent/earned by every tier — `cumulativeRevenue` is a per-tier informational stat (lifetime Revenue earned from that tier), not a second currency. This preserves the currency-separation principle above.
- UI: `src/components/VentureCard.tsx` (one per tier, mapped from the static `SOCCER_VENTURE_TIERS` config in `src/routes/Home.tsx`) replaced the old single `MatchPanel`/`MatchControls`, which were deleted. No styling pass yet (that's step 9) — just structure/interactivity.

### Payout visibility fix (third amendment to step 2)
Closed the "click with no feedback until match end" gap identified in playtesting: `VentureCard.tsx` now derives a live match-progress percentage (a native `<progress>` element) from `elapsedTicks / DEFAULT_SOCCER_CONFIG.ticksPerMatch`, and a live "if the match ended now" projected-payout line. The projection reuses `getOutcome()` (now exported from `soccerModule.ts`) and `calculateMatchRevenue()` (`src/engine/economy.ts`) — the exact same functions the store calls at real match completion — so the preview can never drift out of sync with the actual payout. Purely presentational at the time it was made; superseded below by an actual economy change (Revenue is no longer only credited at completion).

### Persistence (transparent addition to step 2)
`src/store/useGameStore.ts` is now wrapped in zustand's `persist` middleware (localStorage, key `idle-sports-game-save`), per the "client-side persistence for v1" tech-stack line. `partialize` persists only `{ tiers, currencies }` — actions are always recreated fresh, never serialized. This is transparent to every tier/economy/engine call site above; none of that code changed.

### Direct-click economy model (fourth amendment to step 2 — supersedes "pay at outcome only")
Manual clicks (and equivalently automated ticks — same `tickTier()` path, no fork) now grant direct, immediate Revenue every tick. The match-completion payout is no longer the only source of Revenue; it's an additional bonus stacked on top:
- `src/sports/soccer/soccerModule.ts` adds `BASE_PER_TICK_REVENUE = 4` and `tierPerTickRevenue(config, level)` (`round(BASE_PER_TICK_REVENUE * baseRevenueMultiplier * level)`) to its additive tier-config section — the same scaling factor the completion bonus already uses, so relative tier/level progression stays consistent between the two income sources. `tick()`, `getOutcome()`, `DEFAULT_SOCCER_CONFIG`, and `createSoccerModule()` are unchanged.
- `tickTier()` in the store now adds `perTickRevenue` to both `currencies.revenue` and that tier's `cumulativeRevenue` on **every** tick, not only at completion. On the completion tick, the unchanged completion-bonus formula is added on top (`totalEarned = perTickRevenue + completionBonus`), never replacing it. (`cumulativeRevenue` still accrues every tick as a per-tier informational stat, but — as of the fifth amendment below — no longer drives unlocking.)
- **Balancing reasoning:** assumed 1 click/sec (a conservative, easily-sustained manual pace, slower than the game's own 600ms auto-tick rate). At `BASE_PER_TICK_REVENUE=4`, Local Game/level 1 earns 4 Revenue/tick, so the cheapest first purchase (Improve Training, cost 100) is reachable in `100/4 = 25` ticks ≈ 25s — the middle of the requested 20-30s window.
- **Threshold numbers chosen (originally as auto-unlock thresholds, now reused as unlock purchase costs — see below):** `local-tournament` 600→450, `regional-championship` 3000→2250, `national-league` 15000→11250 (`local-game`'s is unused). The 450→2250→11250 ladder is a clean 5x/5x growth, preserving the pre-existing design intent that unlock costs compound faster than the (unchanged, ~4x/3x/2.9x) multiplier growth per tier.
- UI: both purchase buttons show a clamped "current/cost Revenue" progress format (e.g. `Improve Training (72/100 Revenue)`, capped at the cost once affordable) instead of a flat cost, and a `+N Revenue per push` caption sits under "Push the Attack", computed via the same `tierPerTickRevenue()` helper `tickTier()` uses — never a separate hardcoded number.
- Playtested end-to-end: first Revenue is now visible after exactly one click (previously required a full 90-tick match); the first affordable purchase (Improve Training) now lands at click/second 25 (previously took multiple full matches, hundreds of clicks).

### Tier unlocking as a player purchase (fifth amendment to step 2 — supersedes automatic threshold-unlock)
Unlocking a tier is now a deliberate spend, not something that fires automatically once a stat crosses a threshold:
- `SoccerVentureTierConfig`'s `unlockThreshold` field is renamed `unlockCost` — it's paid from the player's current spendable `currencies.revenue` balance, the exact same pool `upgradeTier`/`hireManagerForTier` draw from. The three cost values are unchanged (450 / 2250 / 11250) — only the trigger mechanism changed, per this amendment's request.
- New store action `unlockTier(tierId)`: no-op if already unlocked or Revenue is insufficient; otherwise deducts `unlockCost` and flips that tier's `unlocked` to `true`. Same shape as `upgradeTier`/`hireManagerForTier` — no new pattern introduced.
- `applyTierUnlocks` (the old every-tick auto-check function) is deleted entirely — `tickTier()` no longer touches unlock state at all, only match/revenue state.
- UI: a locked tier's card now shows an always-rendered "Unlock {name} ({current}/{cost} Revenue)" button, disabled until affordable — identical pattern to the Improve Training/Hire a Manager buttons, rather than a passive progress readout.
- **This is the intended design:** because unlock cost is drawn from the same balance as every other purchase, spending on the current tier directly delays affording the next tier's unlock, and vice versa — a real, felt trade-off, not just numbers on two independent tracks. Playtested and confirmed: buying Local Game's "Improve Training" at Revenue=100 visibly reset "Unlock Local Tournament" progress from 100/450 back to 0/450 in the same instant; later, spending 450 to unlock Local Tournament at Revenue=456 instantly dropped Local Game's own Improve Training/Hire a Manager buttons from affordable back to disabled. Both directions of the tension are directly observable, not theoretical.

### Scoped visual styling pass (sixth amendment to step 2 — not full step 9 art)
A lightweight design system was introduced to make ongoing playtesting legible, without pulling in step 9's illustrated art or any UI library. CSS/markup only — no store, engine, or component-behavior changes.
- **Design tokens** live in `src/index.css` as CSS custom properties (`--color-*`, `--space-*`, `--radius-*`, `--shadow-card`), with a light palette in `:root` and a dark override under `@media (prefers-color-scheme: dark)` — no JS theme toggle, relies on the OS/browser preference like the rest of the app already did (`color-scheme: light dark`).
- **Palette:** blue `--color-primary` for the manual "Push the Attack" action, green `--color-success` for Revenue/purchase buttons, amber `--color-warning` for "locked but affordable," neutral `--color-muted-surface` for "locked and far off." This mapping is deliberate — color communicates state at a glance (active/blue, spend/green, almost-there/amber, not-yet/gray) — and should be reused for any future sport's cards rather than inventing a new scheme.
- **Card states:** `.venture-card--unlocked` (white/dark surface, blue left-border accent), `.venture-card--locked-affordable` (amber-tinted background/border), `.venture-card--locked-far` (muted, reduced opacity) — the exact three states requested, driven by existing `tier.unlocked`/affordability booleans already in the component, not new state.
- **Structure:** `src/components/VentureCard.css` holds card/button/progress styles; `src/App.css` holds page shell/header layout; both consume the shared tokens from `index.css`. The native `<progress>` element was replaced with a custom div-based track/fill (same underlying `progressPercent`/`unlockProgressPercent` values) purely for cross-browser style control.
- Buttons use a shared `.btn` base + role modifier (`--primary`, `--purchase`, `--unlock`) with an explicit disabled style (muted gray, not browser-default), so affordable vs. not is unambiguous at a glance.
- Verified: build/lint clean, `git diff` confirms zero changes under `src/store/`, `src/engine/`, `src/hooks/`, `src/sports/`; checked visually in both light and dark color schemes via the browser preview tool.

### Reset Progress button (seventh amendment to step 2)
Added a player-facing way to wipe their save without needing browser devtools, since the Claude Browser pane's `localStorage` is a separate context from the player's own browser and can't be cleared remotely.
- New store action `resetProgress()` in `useGameStore.ts`: sets `tiers` back to `createInitialTiers()` and `currencies` back to `{ revenue: 0 }`. The `persist` middleware picks up this `set()` the same as any other action, so it re-saves the fresh state to `localStorage` automatically — no separate storage-clearing code needed.
- UI: a small muted "Reset Progress" button in the page header (`Home.tsx`/`App.css`, `.app-header__reset`), styled to recede visually (thin outline, muted text, turns red-ish only on hover) rather than compete with the primary Revenue display. Guarded by a native `window.confirm()` before calling `resetProgress()` — cheap, no new dependency, prevents an accidental misclick from wiping progress.
- Verified: reset correctly zeroes all tiers/currencies and rewrites the `localStorage` entry; canceling the confirm dialog leaves progress untouched.

### Ladder extended to 6 tiers (eighth amendment to step 2 — mechanical extension, not a new balance pass)
Added tiers 5-6 (`Continental Cup`, `World Championship`) to `SOCCER_VENTURE_TIERS`, continuing the exact curve established for tiers 1-4 — no new mechanics, no changes outside the config array itself.
- **Confirmed the pattern is genuinely config-driven:** `git diff --stat` showed only `soccerModule.ts` changed. `Home.tsx`, `VentureCard.tsx`, `useGameStore.ts`, and `useMatchTicker.ts` all iterate/index generically over `SOCCER_VENTURE_TIERS` with no hardcoded tier count anywhere (verified by grep before making the change) — the 6-card UI, independent per-tier state, and unlock/upgrade/manager purchases all worked with zero code changes beyond the two new config entries.
- **Numbers chosen (mechanical continuation, not a new decision):**
  - `unlockCost`: continues the exact 5x/5x ladder — 11250 → **56250** → **281250**.
  - `managerHireCost`: also continued at exactly 5x (its tier 1-4 ratios were 3.33x/5x/4.8x, already trending toward 5x) — 12000 → **60000** → **300000**.
  - `upgradeBaseCost`: continued at ~4.2x (tier 1-4 ratios were 3x/4x/4.17x, trending toward ~4.2x) — 5000 → **21000** → **88200**.
  - `upgradeCostGrowth`: continued the +0.05-per-tier pattern (1.6, 1.6, 1.65, 1.7) — → **1.75** → **1.8**.
  - `baseRevenueMultiplier`: continued the gently-decreasing ratio sequence (4x, 3x, 2.917x) — 35 → **100** (2.857x) → **280** (2.8x). Slightly below the "~3-4x" characterization for tier 6, but this is the faithful continuation of the already-decreasing trend, not a deviation from it.
- Tiers 7-11 and prestige are explicitly out of scope for this session, per instruction — not referenced or scaffolded anywhere.
- Verified: build/lint clean; all 6 cards render correctly (screenshot-checked); unlocked Local Tournament live to confirm a newly-added-pattern-tier's neighbors (Regional Championship through World Championship) still correctly track the shared Revenue balance with no chain dependency on any specific prior tier.

### Prestige system: "Reset for Legacy" (step 7 — new architectural layer, not a tier extension)
Adventure-Capitalist-style prestige: trade this run's progress for a small, permanent currency and a handful of lasting upgrades. Deliberately built as its own layer (`src/engine/prestige.ts` + `LegacyState` in the store + `LegacyPanel.tsx`), not bolted onto the venture-tier code — tiers/economy/engine files from steps 1-2 above are unchanged by this step.
- **Trigger:** `resetForLegacy()` (`src/store/useGameStore.ts`) is a no-op unless `FIRST_PRESTIGE_TRIGGER_TIER_ID` (`'world-championship'`, exported from `soccerModule.ts`) is unlocked. The UI (`LegacyPanel.tsx`) shows a locked explainer until then, matching the venture-tier lock-card convention rather than hiding the system outright. ~~Originally checked by array position (`tiers[tiers.length - 1].unlocked`), reasoned to "stay correct if the ladder grows."~~ **That reasoning was wrong** — see the ninth amendment below, which appended 5 more tiers and broke this exact assumption; the trigger is now looked up by id specifically, not by position.
- **Legacy Points formula:** `calculateLegacyPoints(totalEarnings) = floor(sqrt(totalEarnings) / 10)` (`src/engine/prestige.ts`), where `totalEarnings` is the sum of every tier's `cumulativeRevenue` since game start or the last reset. Square-root scaling means early revenue counts for more per-point than the huge late-game numbers, so points keep climbing (not stalling) across repeated future prestiges.
  - **Constant derivation:** simulated a greedy "always spend on the most useful thing" playthrough of the real soccer ladder (unlock next tier > hire manager > buy training, in that priority, against 1.667 auto-ticks/sec once a manager's hired + 1 manual click/sec on the frontier tier) to estimate realistic total lifetime earnings at the moment a player first unlocks World Championship. Three scenarios: no managers/no upgrades (mathematical floor, ≈352k, ~59 LP), managers-but-no-upgrades (≈429k, ~65 LP), managers-and-upgrades (≈990k, ~99 LP). Picked the scaling constant `10` so this whole realistic range maps to **~59-99 Legacy Points** on a first prestige — enough to buy 1-2 of the cheaper permanent upgrades below, not the whole tree, matching the "meaningful but not trivializing" brief.
  - The live "Reset for Legacy" preview in `LegacyPanel.tsx` calls the exact same `calculateLegacyPoints()` the store's `resetForLegacy()` uses — same drift-proof preview pattern as the third amendment's match-payout projection above.
- **On confirming reset:** `resetForLegacy()` adds the computed Legacy Points to `legacy.legacyPoints`, sets `legacy.hasPrestiged = true` and increments `legacy.prestigeCount`, then rebuilds `tiers`/`currencies` via the same `createInitialTiers()`/`startingRevenue()` helpers a brand-new game uses (parameterized by the *current* permanent upgrades — see below). `legacy.legacyPoints`/`hasPrestiged`/`prestigeCount`/`permanentUpgrades` are never touched by this reset. `hasPrestiged` gates tiers 7-11's existence in the UI — see the ninth amendment below.
- **Permanent upgrade tree** (`PERMANENT_UPGRADES` in `src/engine/prestige.ts`, purchased via `purchaseLegacyUpgrade()`), kept to 4 small effects expressed generically (a revenue multiplier, a starting-capital amount, a pre-unlocked-tier count, an unlock-cost discount) so a second sport's ladder benefits with zero changes here:
  - ~~**Revenue Boost** — +10% global Revenue (per-tick and completion) per level, 5 levels, cost `20 × level` LP (20/40/60/80/100).~~ **The 5-level cap was removed** in the thirteenth amendment (Block B) — now scales indefinitely, same cost formula. Applied in `tickTier()`/`VentureCard.tsx` via `globalRevenueMultiplier()`.
  - **Head Start Capital** — one-time, 15 LP: every future run starts with 500 Revenue already banked (`startingRevenue()`).
  - **Fast Track** — one-time, 40 LP: Local Tournament starts pre-unlocked on every future run (`startingUnlockedTierCount()`).
  - ~~**Veteran Discount** — -5% to every tier's unlock cost per level, 3 levels, cost `35 × level` LP (35/70/105). Fully maxing all four costs 565 LP total — a long-term, many-prestiges goal, not a first-run one.~~ **The 3-level cap was removed** in the thirteenth amendment (Block B) — now scales indefinitely toward a 75%-off asymptote that can never be reached. Applied in `unlockTier()`/`VentureCard.tsx` via `unlockCostMultiplier()`.
  - All four are additive/no-op with zero owned (levels 0/false) so a player who never prestiges sees byte-for-byte the same behavior as before this step.
- **UI:** `src/components/LegacyPanel.tsx` + `LegacyPanel.css`, rendered above the venture-tier list in `Home.tsx`. Deliberately a different hue family (`--color-legacy*` purple/gold tokens in `index.css`, light + dark) from the tier cards' blue/green/amber, so it reads as a distinct currency/system at a glance. The reset button is guarded by a native `window.confirm()` detailing the Legacy Points about to be gained and what gets wiped — same cheap, no-new-dependency pattern as the existing Reset Progress button (seventh amendment above), appropriate given this is also a destructive-to-run-progress action.
- **Distinct from the existing "Reset Progress" debug button:** that button (`resetProgress()`) is a full wipe INCLUDING Legacy — it now also resets `legacy` back to `createInitialLegacy()`, so it stays a true "brand new player" reset rather than a confusing half-reset. `resetForLegacy()` is the only path that preserves Legacy Points/upgrades.
- **Playtested first-prestige walkthrough (the actual test of whether this works):** seeded a run with 700,000 total earnings (mid-point of the simulated range) and unlocked World Championship → preview correctly showed "83 Legacy Points" → confirmed reset → tiers/Revenue wiped to fresh-game state, `legacyPoints` became 83. Spent 20 LP on Revenue Boost lvl 1 + 15 LP on Head Start Capital (35 of 83 spent, 48 banked for next time) and separately verified Fast Track. Starting a new run afterward: Revenue began at 500 (vs. 0) and Local Tournament was already unlocked (vs. requiring a 450-Revenue grind) — both immediately felt, real head-starts — while Regional Championship onward still require the same near-full unlock grind as a first run, just ~10% faster forever from Revenue Boost and (if Veteran Discount is later bought) slightly cheaper unlocks. This is exactly the intended shape: a felt boost to the early game, not a skip of the whole run.
- Verified: build/lint clean; exercised `resetForLegacy`, all 4 `purchaseLegacyUpgrade` branches, and `resetProgress`'s full-wipe-including-Legacy behavior directly against `localStorage` state in the browser preview (native `confirm()` dialogs are auto-suppressed in the preview tool's automation, so purchase/reset actions were confirmed by patching `window.confirm` for verification purposes only — real players get the normal browser dialog). Caught and fixed one real bug during this verification: `VentureCard.tsx`'s locked-tier unlock-cost display and the unlocked-tier per-tick/projected-payout previews were reading raw `soccerModule.ts` config numbers and hadn't been updated to apply the new Legacy multipliers, so they'd have drifted from what the store actually charged/granted once any Revenue Boost or Veteran Discount was owned — fixed by routing all three through the same `globalRevenueMultiplier()`/`unlockCostMultiplier()` helpers the store uses.

### Post-prestige ladder: tiers 7-11 (ninth amendment to step 2 — mechanical extension + reveal mechanic)
Added `Legends' Circuit`, `Galactic League`, `Mythic Ascension`, `Eternal Championship`, `The Multiverse Cup` to `SOCCER_VENTURE_TIERS` (`src/sports/soccer/soccerModule.ts`), continuing the exact tier 1-6 curve — no new balance philosophy, same mechanical-continuation approach as the eighth amendment. New this time: these 5 tiers must not exist/render/be referenced in the UI at all until a player's first prestige, then stay revealed permanently for that save (including across every later prestige reset).
- **Numbers chosen (mechanical continuation):**
  - `unlockCost`/`managerHireCost`: continue the exact 5x ladder — unlockCost 281250 → **1,406,250 → 7,031,250 → 35,156,250 → 175,781,250 → 878,906,250**; managerHireCost 300000 → **1,500,000 → 7,500,000 → 37,500,000 → 187,500,000 → 937,500,000**.
  - `upgradeBaseCost`: continues the exact 4.2x ratio already stable across tiers 5-6 — 88200 → **370,440 → 1,555,848 → 6,534,562 → 27,445,160 → 115,269,672**.
  - `upgradeCostGrowth`: continues the +0.05-per-tier pattern — 1.8 → **1.85 → 1.9 → 1.95 → 2.0 → 2.05**.
  - `baseRevenueMultiplier`: continues the gently-decreasing ratio (was 4x/3x/2.917x/2.857x/2.8x) at -0.05 per further step — 280 → **770 (2.75x) → 2079 (2.70x) → 5509 (2.65x) → 14323 (2.60x) → 36524 (2.55x)**.
- ~~**Reveal mechanic:** `TIERS_REVEALED_BEFORE_PRESTIGE` (`soccerModule.ts`) is derived from `FIRST_PRESTIGE_TRIGGER_TIER_ID` (`SOCCER_VENTURE_TIERS.findIndex(...) + 1`, currently `6`) rather than a second hand-maintained number, so the two constants can't silently drift apart. `Home.tsx` slices `SOCCER_VENTURE_TIERS` to this length unless `legacy.hasPrestiged` is true, in which case it renders the full 11.~~ **Superseded** — see the thirteenth amendment's Block A: tiers 7-11 now reveal one at a time per completed prestige (`revealedTierCount(prestigeCount)`), not all 5 together the instant `hasPrestiged` flips true. The underlying `TIERS_REVEALED_AT_START` constant (renamed) is still derived from `FIRST_PRESTIGE_TRIGGER_TIER_ID` the same way.
- **Defense in depth — the reveal boundary is enforced in the store, not just the render slice:** a new `isTierRevealed(tierIndex, hasPrestiged)` helper (`soccerModule.ts` — signature later changed to take `prestigeCount` directly, see the thirteenth amendment) is checked inside `tickTier`/`upgradeTier`/`hireManagerForTier`/`unlockTier` in the store, not only in `Home.tsx`. This was **not** the original design — an adversarial review caught that, without it, a hand-edited `localStorage` save (this is a client-only game with no backend/validation) could flip a hidden tier's `unlocked`/`managerHired` flags directly and then earn real, spendable Revenue from it via the auto-ticker, before ever prestiging — inflating the very first-prestige Legacy Points payout the constant in the "Prestige system" section above was tuned against. Fixed by making every store action that touches a tier check `isTierRevealed()` itself, so a hidden tier is fully inert regardless of how its flags got set, not just invisible.
- **Save-migration fix:** zustand's default `persist` merge is a shallow spread, so an existing player's persisted 6-entry `tiers` array would have replaced the freshly-initialized 11-entry array wholesale on load, leaving `tiers` shorter than `SOCCER_VENTURE_TIERS` (and crashing on render for anyone who'd already prestiged). Fixed with a custom `merge()` in the persist config (`useGameStore.ts`) that pads any missing trailing tier-state entries with fresh ones, preserving all existing progress for tiers 0-5.
- **`FIRST_PRESTIGE_TRIGGER_TIER_ID` fix (this amendment's other reason for existing):** `resetForLegacy()` and `LegacyPanel.tsx` used to find "the final tier" via `tiers[tiers.length - 1]`. The instant tiers 7-11 were appended, "the final tier" became The Multiverse Cup — reachable only *after* a first prestige — which would have made a first prestige permanently impossible. Both now look up `FIRST_PRESTIGE_TRIGGER_TIER_ID` (`'world-championship'`) by id instead, immune to the ladder's length.
- **"Reset Progress" → "DEV: Wipe All Data":** the header's full-wipe debug button was renamed and re-worded (`Home.tsx`/`App.css`) — dashed border, monospace label, explicit confirm text ("this wipes EVERYTHING... AND your Legacy Points... this is not the normal prestige reset") — so it can't be mistaken for `LegacyPanel`'s "Reset for Legacy" now that a real player might see both. Chose relabeling over removal: it's still the only way to fully clear a save without browser devtools (per the seventh amendment), just clearly marked as a developer affordance now.
- Per instruction, no second prestige layer and nothing gated behind reaching tier 11 — out of scope for this session.
- **Verified:** build/lint clean. Confirmed via an isolated Node harness importing the real store module directly (bypassing browser/localStorage flakiness encountered during interactive testing): `resetForLegacy()` no-ops before World Championship is unlocked; a first prestige with 700,000 total earnings correctly grants 83 Legacy Points and reveals tiers 7-11 (freshly locked); a *second* `resetForLegacy()` correctly requires only World Championship again, not The Multiverse Cup; attempting `tickTier`/`upgradeTier`/`hireManagerForTier`/`unlockTier` on a hand-flipped hidden tier before any prestige is now fully inert (no Revenue earned, no state changed); the `merge()` migration correctly preserves an old 6-tier save's progress while padding tiers 7-11 fresh, across first-load, pre-prestige-system, and already-11-tier-save cases. Also confirmed via the DOM that tier 7-11 names never appear in rendered text before a prestige.
- **Playtest sanity check (post-prestige climb through tiers 7-11):** simulated a continuously-engaged player (same greedy unlock>manager>upgrade policy and tick rates as the original prestige-tuning simulation) climbing the full 11-tier ladder. No Legacy upgrades: ~64 minutes of continuous engagement to reach The Multiverse Cup. With a modest first-prestige upgrade set (Revenue Boost lvl 1, Head Start Capital, Fast Track): ~65 minutes — a small, expected effect since these are calibrated as an early-game boost, not an end-game one (per the "Prestige system" section above). With a heavily-invested multi-prestige upgrade set (Revenue Boost lvl 5, Veteran Discount lvl 3): ~40 minutes, a meaningful ~37% reduction. No discontinuity or unreachable wall going from tier 6 into tier 7 — the curve extends the same relative pacing tiers 1-6 already had, just at a higher absolute scale, so a second playthrough's added depth feels like "a lot more of the same game," not a different game or a brick wall.

### Tier renaming + placeholder icons (tenth amendment to step 2 — cosmetic only, no logic changes)
Renamed all 11 tiers' display `name` (config `id`s, mechanics, and every cost/multiplier are byte-for-byte unchanged) to lean into the ladder's grounded → epic → absurd arc, and gave each a single-emoji placeholder icon standing in for real AI-generated icon/sprite art (step 9):

| # | id | name | icon |
|---|----|------|------|
| 1 | `local-game` | The Sunday League | ⚽ |
| 2 | `local-tournament` | The Corner Kick Cup | 🚩 |
| 3 | `regional-championship` | The Regional Rumble | 🥉 |
| 4 | `national-league` | The National Cup | 🏅 |
| 5 | `continental-cup` | The Continental Clash | 🌍 |
| 6 | `world-championship` | The World Crown | 👑 |
| 7 | `legends-circuit` | The Legends' Gauntlet | ⚔️ |
| 8 | `galactic-league` | The Interstellar Invitational | 🚀 |
| 9 | `mythic-ascension` | The Mythic Ascension | 🐉 |
| 10 | `eternal-championship` | The Eternal Crown | ♾️ |
| 11 | `multiverse-cup` | The Multiverse Cup | 🌌 |

- `SoccerVentureTierConfig` gained one field, `icon: string`. `VentureCard.tsx` renders it in a new `.venture-card__title-group` wrapper (icon + title, `VentureCard.css`) for both the locked and unlocked card layouts — the existing lock-icon/AUTO-badge on the header's far side are unaffected.
- **Every reference to a tier's display name elsewhere is already derived, not hardcoded** (confirmed by grep) — `LegacyPanel.tsx`'s lock-message and reset-confirm text both read the live `config.name` off `SOCCER_VENTURE_TIERS`, so both updated automatically. The one hardcoded exception found and fixed: `PERMANENT_UPGRADES.fastTrack.description` in `src/engine/prestige.ts` had "Local Tournament" typed as a literal string; updated to "The Corner Kick Cup" to match.
- **These icons are explicitly a placeholder**, not the final v1 asset — step 9 ("UI polish + integrate final art assets") is where they get replaced with real AI-generated, style-locked 2D icon/sprite art per the Tech Stack line above. Anyone doing that work should remove `.venture-card__tier-icon`'s emoji rendering path in favor of an `<img>`/sprite reference at that point, not layer real art on top of the emoji.
- Verified: build/lint clean; screenshot-checked in the browser preview that all 6 visible (pre-prestige) cards render their new name + icon correctly and the Legacy panel's dynamically-derived "Reach and unlock The World Crown" text updated with zero code changes to that string itself.

### Achievements: generic badge framework (eleventh amendment to step 2 — new system, not a tier/prestige extension)
A third meta-system, deliberately architected as its own layer (`src/engine/achievements.ts` + `LifetimeStats`/`AchievementsState` in the store + `AchievementsPanel.tsx`) — no changes to tiers/economy/prestige files.
- **Framework:** `AchievementConfig = {id, name, statTracked, threshold, tier, reward}`, where `statTracked` is a plain string key into a `Record<string, number>` stats record (not an enum), and `tier` is a plain string rather than a closed union — deliberately so a future achievement line or a 4th badge tier (e.g. "Platinum") never requires widening a type here, only adding data. `reward.type` IS a closed union (`'revenue' | 'legacyPoints'`, only two reward kinds exist by design), but every place that branches on it (`useGameStore.ts`'s reward loop, `AchievementsPanel.tsx`'s `rewardLabel`) uses an exhaustive `switch` ending in `assertNeverRewardType()` — so adding a third reward kind without updating both is a compile error, not a silently-wrong-currency-granted bug. `checkNewlyEarnedAchievements(stats, earnedIds)` is pure: it returns which not-yet-earned `ACHIEVEMENTS` now qualify, applies no rewards, mutates nothing — the store is solely responsible for granting.
- **First achievement line — total wins across every venture tier combined (not per-tier):** Bronze 50 wins → +200 Revenue; Silver 250 wins → +2000 Revenue; Gold 1000 wins → +10 Legacy Points.
  - **Threshold derivation:** simulated the same greedy always-be-spending policy used to tune Legacy Points (unlock next tier > hire manager > train), this time tracking wins, using an empirically-measured ~53% per-match win rate (simulated 200k matches against the actual tick probabilities). Under that hyper-engaged model: 50 wins ≈ 15.6 min, 250 wins ≈ 67 min, 1000 wins ≈ 260 min (~4.3 hours). A 50→250→1000 ladder is a clean 5x/4x escalation, matching the ratio style already used for tier costs elsewhere in this file. Real, less-than-maximally-optimized play takes meaningfully longer at every threshold — intentional: Bronze is "your first solid session," Silver is "you're invested," Gold is "hours of real cumulative play," not literal-minutes milestones.
  - **Reward derivation:** Bronze/Silver deliberately stay small relative to the same currency they're paid in (200/2000 Revenue vs. contemporaneous unlock costs of 450/11250+) — a nudge, not a shortcut. Gold pays in Legacy Points instead of Revenue specifically to bridge the two currency systems: 10 LP is enough to almost afford Head Start Capital (15 LP) outright, giving even a player who hasn't prestiged yet a first taste of the permanent currency.
  - **Scope decision, documented rather than left silent:** an achievement's Revenue reward lands directly in `currencies.revenue` (spendable immediately) but is NOT added to any tier's `cumulativeRevenue`, so it does not count toward that run's eventual Legacy Points at prestige (`resetForLegacy()` sums only `tiers[].cumulativeRevenue`). This is consistent with `cumulativeRevenue`'s documented per-tier semantics (an achievement is a cross-tier, lifetime accomplishment, not attributable to the one tier whose match happened to trigger it) — but it is a deliberate boundary, not an oversight: two players with identical tier progress will get identical Legacy Points on prestige even if one of them also banked a Bronze/Silver achievement along the way.
- **Lifetime, not per-run:** `lifetimeStats.totalWins` and `achievements.earnedIds` (`useGameStore.ts`) live outside `tiers`/`legacy.permanentUpgrades`, with the exact same persistence semantics as `legacy.legacyPoints`/`hasPrestiged` — never reset by `resetForLegacy()` (an achievement is a lifetime accomplishment; wiping badge progress on every prestige would be actively punishing, not motivating), only by the full `resetProgress()` dev wipe. Deliberate, not the only option available: per-tier `cumulativeRevenue`/`matchesCompleted` ARE reset every prestige despite their own "lifetime" doc comments — but a badge system specifically needs badges to stay earned.
- **Granting is atomic and runs on every tick, not just match completions:** a shared `applyEarnedAchievements()` helper is called from BOTH of `tickTier()`'s branches — the match-completion branch (where `totalWins` actually changes) AND the plain per-tick branch (where it currently doesn't). This was a deliberate fix during review: the first draft only checked achievements at completion, which happened to work for a completion-only stat like wins, but would have silently under-served the framework's own documented example of a future line ("total Revenue earned") — Revenue changes on *every* tick, not just at completion, so an achievement-check wired into only one branch would leave such a threshold unrecognized for up to `ticksPerMatch` ticks after it's actually crossed. Both branches now call the same generic check symmetrically, so a future per-tick stat is caught immediately. Each call applies every newly-earned reward (to `currencies.revenue` and/or `legacy.legacyPoints`) within the same `set()`, so the UI never renders a frame where a badge shows earned but its reward hasn't landed. Correctly handles multiple simultaneous grants (e.g. a hand-edited save jumping straight past all three thresholds in one tick earns all three at once, with all three rewards applied).
- **Extending this later — honestly scoped:** a new achievement line (total Revenue earned, matches played, prestige count, ...) is *mostly* config: new entries in `ACHIEVEMENTS`/`STAT_LABELS` in `achievements.ts`. Being precise rather than overselling it: a genuinely new tracked stat still needs its own field + increment site(s) (in `LifetimeStats` if it must survive prestige, exactly like `totalWins`) plus one line in `AchievementsPanel.tsx`'s `stats` record mapping that field to the key `statTracked` expects — that plumbing is unavoidable since the stat has to be tracked somewhere, and an adversarial review specifically flagged the earlier "just add config" framing as stronger than what the code actually delivers. `prestigeCount` is a rare exception needing *none* of that extra wiring, since it already lives in `legacy.prestigeCount` with the right lifetime persistence — a "reached N prestiges" line really would be pure config.
- **UI:** `src/components/AchievementsPanel.tsx` + `.css`, rendered in `Home.tsx` between `LegacyPanel` and the venture-tier list. A third distinct hue (cyan, `--color-achievement*` in `index.css`) from both the tier cards and Legacy's purple/gold — shifted from an initial teal after review flagged it as only ~19° of hue apart from `--color-success` (too close to read as clearly distinct); cyan sits a safer ~37° away. Individual badge chips use conventional bronze/silver/gold swatches (`--color-badge-*`); silver was likewise reworked from a near-identical match to `--color-text-muted` into a distinctly cooler, lighter steel-blue. An explicit cyan fallback (`.achievement-badge--other`) styles any future tier name outside those three, so "extensible beyond Bronze/Silver/Gold" holds visually, not just in the type system. Achievements are grouped by `statTracked`, so a second line just adds a second group with zero changes to the grouping logic.
- **Playtested honestly — does this add anything in a new player's first few minutes?** Manually clicked "Push the Attack" ~38 times to afford Local Game's first manager hire (~1-2 minutes of realistic new-player clicking), then let it auto-tick for roughly 4 more real minutes total. Result: **2 of 50 wins** — a small but genuinely-moving, non-zero progress bar, exactly in line with the ~15.6-minute Bronze estimate above (a single-tier, non-hyper-optimized player is slower than the multi-tier greedy simulation). Honest verdict: it does **not** feel empty or ignorable — all three badge tiers, their thresholds, and their concrete rewards are visible and legible from the very first render, and the Gold tier's "+10 Legacy Points" line quietly foreshadows the whole prestige system before a player has even unlocked it. But Bronze is genuinely **not** a first-few-minutes payoff for a normal (non-optimized, single-manager) player — it's closer to a first-session milestone. If a snappier "hooked in five minutes" moment is wanted later, the right fix is a *separate* achievement line with a much lower entry threshold (e.g. "first win," or "10 wins") — not lowering this line's Bronze to something below 50, which is calibrated as this line's real entry point, not the game's.
- **Verified:** build/lint clean. An adversarial code-review workflow (4 dimensions, 24 agents including verification) raised 21 findings and confirmed 11 as real; all medium/high-severity ones were fixed (the completion-only achievement check, the non-exhaustive reward-type branching, the Silver/teal color collisions) as detailed above; the low-severity ones (the cumulativeRevenue scope decision, this section itself being newly written) are addressed by documentation rather than code, per their own "plausibly intentional"/"expected, not yet done" framing. Re-verified reward-granting/reset semantics after the fixes via an isolated Node harness importing the real store module directly (same technique as the tiers 7-11 session): crossing 50 wins grants Bronze exactly once, with no double-grant on a subsequent non-completion tick; jumping straight to 1000 wins grants all three tiers simultaneously with exactly correct amounts (+200 and +2000 Revenue, +10 Legacy Points); `resetForLegacy()` leaves `lifetimeStats`/`achievements` completely untouched across a prestige; `resetProgress()` correctly wipes both to zero/empty. Confirmed the new Silver/cyan colors via `getComputedStyle` in the running app: Silver now renders as a distinct steel-blue rather than the previous near-identical muted gray, and the achievements panel's cyan title is clearly distinct from the tier cards' green.

### Settings page + "First Win" achievement (twelfth amendment to step 2 — two small follow-up fixes, no redesign)
Two focused fixes, both to systems built in earlier amendments above.
- ~~**DEV button moved to a real Settings page.** It was relabeled/hardened in the ninth amendment but stayed in the main header — a real player could still see it one tap away from actual play. Now: `src/routes/Settings.tsx` (registered as `/settings` in `App.tsx`) holds it, reachable via a small ⚙ link in `Home.tsx`'s header (`.app-header__settings-link`, top-right corner). Exact same dashed-border/monospace `.app-header__reset` styling and the same explicit "DEV RESET: this wipes EVERYTHING... AND your Legacy Points..." confirm text as before — only its location changed, not its behavior.~~ **Superseded** — see the eighteenth amendment below: the standalone `/settings` route and `src/routes/Settings.tsx` were removed entirely; the DEV wipe button now lives directly in the Franchise tab.
  - ~~**`useMatchTicker()` moved from `Home.tsx` up to `App.tsx`**, called unconditionally regardless of route. It was previously mounted only inside `Home`, so navigating to a `Settings` route would have unmounted it and paused every manager-hired tier's auto-play for as long as the player stayed on that page — wrong for an idle game, where the whole point is that progress continues whether or not you're looking at the main screen. Verified directly: seeded a tier with a manager hired, navigated to `/settings`, waited 15 real seconds, confirmed via `localStorage` that `tickIndex` advanced (0 → 60 of 90) and Revenue accumulated (240) the entire time the Settings page was showing.~~ **`useMatchTicker()` still lives in `App.tsx`, unconditionally above everything** — that part of this decision stands unchanged; only the routing detail it was reasoning about (a `/settings` route) no longer exists. Re-verified against the new tab structure in the eighteenth amendment below.
- **New achievement line entry — "First Win"** (`src/engine/achievements.ts`): `{ id: 'total-wins-first', tier: 'Rookie', threshold: 1, reward: { type: 'revenue', amount: 20 } }`, added to the existing `totalWins` line. Directly answers the eleventh amendment's own honest playtest finding (Bronze's 50-win threshold has no payoff in a brand-new player's first session). Deliberately tiered as **`'Rookie'`, not `'Bronze'`** — the ask was explicit that Bronze/Silver/Gold must keep meaning real, deliberately-paced milestones; a second thing called "Bronze" would have cheapened that. Reward is 1/10th of Bronze's (20 vs. 200 Revenue) — modest, proportional to how little a player has actually done to earn it.
  - **Genuinely a pure config addition, as the framework promised:** zero changes to `useGameStore.ts`, `AchievementsPanel.tsx`, or any CSS file. `AchievementsPanel.tsx`'s `TIER_BADGE_CLASS` map only has entries for Bronze/Silver/Gold and falls back to `.achievement-badge--other` for anything else — the exact generic fallback built (and specifically flagged by the prior session's adversarial review) for this exact situation. `'Rookie'` renders through that fallback with the panel's own cyan accent, no code touched.
  - Verified via the same isolated-Node-harness technique as prior sessions: crossing 1 win grants exactly `total-wins-first` (+20 Revenue) and nothing else; later crossing 50 wins still grants Bronze correctly, with `earnedIds` accumulating `[total-wins-first, total-wins-bronze]` and no duplicates. Confirmed in the browser preview that the Achievements panel's "next unearned" progress line now reads `0 / 1` for a fresh save (previously `0 / 50`), and all 4 badges (Rookie/Bronze/Silver/Gold) render correctly.
- Verified: build/lint clean.

### Prestige reveal cadence, uncapped Legacy sinks, training-gated match escalation, and milestone multipliers (thirteenth amendment to step 2)
Four related balance/pacing changes agreed on in design discussion, done together in one session because they interact (per-tier training level, in particular, is now touched by three of the four). No build-order step changed — this is entirely within step 2's existing systems (venture tiers, prestige, achievements were all already done).

**Block A — one hidden tier revealed per prestige, not all 5 at once.** Superseded the ninth amendment's "`hasPrestiged` reveals all of tiers 7-11 together":
- `revealedTierCount(prestigeCount)` (`soccerModule.ts`, replaces `TIERS_REVEALED_BEFORE_PRESTIGE`) = `TIERS_REVEALED_AT_START` (6) + `min(prestigeCount, MAX_POST_PRESTIGE_REVEALS)` (5) — tier 7 (`legends-circuit`) reveals after prestige #1, tier 8 after #2, ... tier 11 after #5, and reveals stop there (a 6th+ prestige only earns more Legacy Points, reveals nothing new). `isTierRevealed(tierIndex, prestigeCount)` takes the count directly now, not the old boolean — every call site in `useGameStore.ts` (`tickTier`/`upgradeTier`/`hireManagerForTier`/`unlockTier`) and `Home.tsx`'s render slice was updated to pass `legacy.prestigeCount`.
- No minimum time/earnings gate between prestiges, per instruction — a player rushing through resets quickly reveals tiers just as fast; that's an accepted trade-off (they're also giving up more mid-run progress each time), not something worth adding friction to prevent.
- `LegacyPanel.tsx` copy now names the *specific* next tier ("Prestiging again will permanently reveal The Legends' Gauntlet (4 more tiers to reveal after that)") instead of implying a single all-at-once unlock, both in the locked-explainer text and the reset-confirmation dialog.
- **`hasPrestiged` still exists** (gates the Legacy panel's locked-vs-reset-block UI) but no longer drives the reveal boundary — `legacy.prestigeCount` does. Both are lifetime fields, never reset by `resetForLegacy()`, only by `resetProgress()`'s full wipe.
- **Save-compatibility note, deliberate not a bug:** any save that already had `prestigeCount >= 1` under the OLD "reveal all 5" rule will, after this change, only see `revealedTierCount(prestigeCount)` tiers (e.g. a save with `prestigeCount: 1` now sees only tier 7, not all of 7-11). Any tiers 8-11 state that save already had (level/unlocked/cumulativeRevenue) is NOT deleted — it just becomes inert/hidden again until later prestiges re-reveal it — and gets wiped on the save's next prestige regardless, same as any tier's progress always has. This is the correct, intended consequence of the requested rule change (there is no way to "reveal exactly one tier per prestige" without an existing multi-prestige save's visible count changing to match), not something migration code should paper over.

**Block B — Revenue Boost and Veteran Discount scale indefinitely (`prestige.ts`).** `maxLevel` removed from both; `PERMANENT_UPGRADES.revenueBoost`/`veteranDiscount` no longer have the field, and `purchaseLegacyUpgrade()`/`LegacyPanel.tsx` no longer check or render a "MAXED" state for either (Head Start Capital/Fast Track are unchanged one-time toggles).
- **Revenue Boost:** cost formula unchanged (`20 × nextLevel` — already a valid indefinite-scaling formula, just previously artificially capped at level 5). This is an economic soft cap by design: technically infinite, practically self-limiting because cost keeps climbing every level forever while the benefit stays a flat +10%/level.
- **Veteran Discount — required a genuine mathematical asymptote, not just a soft cap, since a 100% discount would make unlocking free and break the economy:** `unlockCostMultiplier(level) = (1 - MAX) + MAX × RATE^level`, with `MAX = 0.75` (asymptote: never more than 75% off) and `RATE = 0.9` (each level closes 10% of the *remaining* gap to that asymptote, not 10% of the original cost). The discount is `MAX × (1 - RATE^level)`. This is safe by construction, not by floating-point luck: the multiplier is `floor + MAX × RATE^level` where `floor = 1 - MAX = 0.25` is added as its own fixed term, so even if `RATE^level` underflows to exactly `0.0` at extreme levels (verified via harness: happens by level ~15,000), the multiplier still evaluates to exactly `0.25`, never below it — the 100%-discount case is structurally unreachable, not merely improbable. Verified: multiplier stays `> 0.25` at level 100, `== 0.25` (the floor, not below) at levels 100,000 and 1,000,000.
- Cost formula also unchanged (`35 × nextLevel`) — the asymptote's own diminishing returns are what keeps very high levels not worth grinding for, so cost doesn't additionally need to gatekeep it.

**Block C — three related match/economy changes, all through the `SportModule<TState>` engine contract:**
1. **Per-tier auto-play timing.** Match length stays a fixed 90 ticks at every tier (unchanged) — only the REAL-WORLD milliseconds between auto-ticks now varies per tier: `autoTickIntervalMsForTier(tierIndex) = round(600 × 1.4^tierIndex)` (`soccerModule.ts`), tier 0 unchanged at 600ms (~54s/auto-match) climbing to ~17.4s at tier 10 (~26min/auto-match) — a ~29x spread. Manual "Push the Attack" clicks never go through this at all (they call `tickTier()` directly from `VentureCard.tsx`), so hand-grinding a high tier is never slowed by this. `useMatchTicker.ts` was rewritten to run one independent `setInterval` PER auto-playing tier at its own pace, rather than one shared interval — see the bug/fix note below for why that rewrite mattered.
2. **Margin-based bonus revenue via a generic "performance factor."** `SportModule<TState>` gained a required `getPerformanceFactor(state, context?): number`, returning a generic 0-1 scale (0 = most lopsided possible loss, 0.5 = neutral/any real draw, 1 = most decisive possible win) — `economy.ts`'s `calculateMatchRevenue(outcome, performanceFactor)` only ever consumes that number, never sport-specific stats. Soccer derives it from clamped goal differential (`MAX_MEANINGFUL_GOAL_DIFFERENTIAL = 5`, simulated: P(|diff|≥5) ≈ 3.2% of matches — a real but genuinely rare blowout tail). The bonus (`decisiveness = max(0, (factor-0.5)×2)`, revenue `= base × (1 + 0.6 × decisiveness)`) only ever applies ABOVE neutral and only ever adds on top of the flat base — a decisive win pays more, but no outcome is ever paid LESS than its own flat base, specifically so there's never a "lose more decisively for more Revenue" exploit. A minimum win (1-0) pays ~+12%, a blowout (5+) pays +60%.
3. ~~**Minimum training level to win.** `MatchContext` (`types.ts`, `{level?, minWinLevel?}`) is an optional capability threaded through `tick()`/`getOutcome()`/`getPerformanceFactor()` — a sport module with no such concept just never reads it, and the engine (`tickEngine.ts`) never interprets it, only passes it through. Soccer's `getOutcome()` downgrades a would-be 'win' to 'draw' outright (hard cliff, not a scaled-down chance) whenever `level < minWinLevel`; `minWinLevelForTier(tierIndex) = tierIndex + 1` (1 for tier 0 — no gating — up to 11 for The Multiverse Cup).~~ **Fully superseded, not just tuned** — see the fourteenth amendment below: the hard cliff is gone entirely, replaced by a continuous, opponent-level-based win probability. `MatchContext.minWinLevel` no longer exists.

**Block D — compounding-doubling training milestones (`soccerModule.ts`).** `trainingEffectMultiplier(level) = level × 2^(milestones passed)`, milestones at `TRAINING_MILESTONE_LEVELS = [6, 13, 22, 34, 50, 70, 95, 125, 160, 200]` (widening gaps: 7, 9, 12, 16, 20, 25, 30, 35, 40) — used everywhere a tier's level scales revenue (`tierPerTickRevenue()`, and the store's/`VentureCard.tsx`'s completion-bonus formulas, replacing the old raw `× level`). Does NOT affect `tierUpgradeCost`, which stays keyed on raw level exactly as before — only the revenue side gets the milestone boost.
- **Thresholds are simulated, not round numbers:** without any milestone boost, this economy's exponential `upgradeCostGrowth` (1.6-2.05× per level) against linear-in-level revenue means the level-up cadence itself grows roughly `g` times slower every additional level, so a fixed absolute level becomes many-days-then-effectively-unreachable within a few dozen levels at EVERY tier (confirmed: reaching level 30 baseline takes ~12.6 days at tier 0, and levels beyond ~25-30 are already outside a 58-day simulation cap at every tier tried) — regardless of a tier's own `baseRevenueMultiplier`, since that only shifts the wall by a roughly-constant number of levels against an exponential, never removes it. A first search tried [10, 25, 50, 100, 200, ...] and failed immediately in simulation (the 10→25 gap alone already cost 17+ hours even with the first doubling active) — the shipped thresholds were tightened until the first several milestones landed within single-digit minutes to about an hour of dedicated single-tier training investment at any tier, matching this session's "first breakpoint fairly early" ask; by design (matching Cookie Clicker's own high-count building milestones widening from every-few to every-50), the later ones (125+) are intentionally long-horizon, many-real-days goals for only the most dedicated single-tier grinding.

**Adversarial review and fixes.** A workflow-based review (6 interaction-focused lenses × adversarial verification, 28 agents total) specifically hunting for cross-block interaction bugs raised 11 candidate findings and confirmed 9; the following were fixed:
- **Root-cause bug, independently flagged by 5 of the 6 lenses:** `getPerformanceFactor()` didn't take `context`, so a `getOutcome()`-downgraded win (Block C3, label now `'draw'`) still reported its real, non-neutral goal-differential factor to `getPerformanceFactor()`, letting Block C2's margin bonus leak into what was paid out as a flat draw — e.g. a 2-0 downgraded win paid 50 Revenue instead of the intended flat 40 (a 25% overpay that scales with tier/level/Legacy Revenue Boost, and inflates `cumulativeRevenue` feeding Legacy Point math). Fixed by giving `getPerformanceFactor()` the same optional `context` parameter as `getOutcome()` (both now share a private `isGatedWin()` check in `soccerModule.ts`, so the two can't drift apart again) — a gated win now reports exactly the neutral `0.5` a genuine draw would. `VentureCard.tsx`'s preview was updated to pass the same context to both calls.
- **`useMatchTicker.ts` interval-starvation bug:** the original per-tier-interval rewrite still tore down and recreated ALL auto-tier intervals whenever the SET of auto-playing tiers changed at all (e.g. hiring a manager for a brand-new low tier). With tiers now at wildly different real-world intervals (600ms to ~17s), this meant a high tier's long-running countdown could be perpetually reset to zero before ever firing, if managers were hired often enough elsewhere. Fixed by diffing against a `useRef` map of tier-id → interval-id and only adding/removing the tiers that actually entered/left the auto-playing set — an unrelated tier's timer is never touched.
- **`LegacyPanel.tsx` off-by-one:** the "N more tiers to reveal after that" count used `legacy.prestigeCount` as-is, but that hasn't incremented yet for the prestige the copy is describing ("prestiging again will reveal X") — it was counting the very tier about to be revealed as still remaining. Fixed to count against `prestigeCount + 1`; also fixed grammar (singular "tier" vs plural "tiers").
- **Reset-for-Legacy dialog clarity:** didn't call out that every tier's training level (not just Revenue/unlocks) resets, which — new as of Block C3 — means wins at previously-trained tiers may resolve as draws again post-reset until retrained. Added a clause to the confirmation text.
- One additional candidate (whether `useMatchTicker`'s auto-tier filter needed its own `isTierRevealed` check) was correctly refuted on verification: `tickTier()` already no-ops for an unrevealed tier as its own defense-in-depth guard (per the ninth amendment), so a phantom interval for a hidden tier would tick nothing, not a real bug.

**Conceptual playtest across all four systems together.** Simulated a greedy (unlock-next > hire-manager > train) single-frontier-tier policy across 6 successive prestiges, spending Legacy Points greedily (Head Start Capital > Fast Track > cheapest of Revenue Boost/Veteran Discount) each time: time-to-reach-World-Crown decreased smoothly run over run (relative pacing units: 8.4 → 6.0 → 5.5 → 5.3 → 5.3 → 4.9) while per-run Legacy Point income stayed roughly flat (~94-104 LP) — a healthy diminishing-returns curve, not a stall (numbers never plateau-then-jump) or a runaway snowball (Revenue Boost's linear-cost/flat-benefit shape and the LP formula's own sqrt-scaling both damp the loop). Combined with Block A's reveal cadence, this means each successive prestige both reveals a new tier AND makes reaching World Crown again modestly faster — matching the "a lot more of the same game, not a brick wall" verdict the ninth amendment already reached for the tiers 7-11 climb itself.
- Verified: build/lint clean; a direct-import Node harness (same technique as prior sessions) covers all four blocks plus the specific regression the adversarial review caught (gated win reports factor `0.5` and pays exactly the flat draw rate, not an inflated one).

### Continuous win-probability model + training milestone visibility (fourteenth amendment to step 2 — the win-probability half REPLACES Block C3 above, not additive)
Two changes requested together. The milestone-visibility one is purely additive UI for an already-shipped mechanic; the win-probability one is a genuine replacement of how match outcomes are decided — Block C3 in the thirteenth amendment above no longer reflects reality and is struck through in place rather than left to quietly contradict this section.

**Milestone visibility (additive UI, no economy/logic change):**
- `nextMilestoneLevel(level)` (`soccerModule.ts`) returns the next not-yet-crossed entry in `TRAINING_MILESTONE_LEVELS`, or `null` once all ten are passed. `VentureCard.tsx` shows a compact "Next: training 2x at Level N (current/N)" line + progress bar next to Improve Training for any unlocked tier that hasn't hit the last milestone yet.
- A toast (`"{tier name} Revenue 2x!"`) fires the instant `upgradeTier()` crosses a milestone level. New store state `notifications: {id, message}[]` + action `dismissNotification(id)`, rendered by a new `src/components/NotificationToasts.tsx` (mounted in `App.tsx`, outside `<Routes>`, so a toast stays visible across a route change rather than being torn down by navigating to Settings mid-toast). Deliberately NOT persisted (absent from `partialize`) — a page reload never resurrects a stale toast from a prior session. Each toast owns its own 4-second dismiss timer starting from when it mounts, not from when the crossing happened — so a toast is never silently skipped, it just gets its full on-screen time whenever it's next actually seen.
- Verified live: crossed the first milestone (Level 1 → 6 at The Sunday League) and watched per-push/per-completion revenue double as designed, the indicator advance to "Next: Level 13," and — timed tightly around a later crossing (Level 21 → 22) — caught the toast actually present in the DOM with the exact expected text. No console errors across several thousand stress-test clicks and 100+ completed matches in the same session.

**Continuous win-probability model — supersedes the thirteenth amendment's Block C3 entirely, not a tuning pass on it:**
- `MatchContext` (`types.ts`) drops `minWinLevel`, gains `opponentLevelRange?: {min, max}`. `opponentLevelRangeForTier(tierIndex)` (`soccerModule.ts`) replaces `minWinLevelForTier` — centered on the exact same `tierIndex + 1` anchor the old cliff used (preserving that number's existing calibration rather than re-deriving it), with a fixed ±2 spread clamped at a floor of 1: tier 0 is `[1,3]`, tier 10 (The Multiverse Cup) is `[9,13]`.
- On a match's FIRST tick only, `tick()` draws that match's opponent level uniformly from this range, computes `gap = opponentLevel - playerLevel`, and resolves the match's TRUE outcome via `P(win) = 1 / (1 + 10^(gap/S))`, `S = 4` — used as a direct, literal probability draw (`Math.random()` against the computed thresholds), not approximated by biasing per-tick scoring rates. `S=4` reproduces the requested anchor exactly (`gap=3` → 15.1% win chance). The formula is mathematically incapable of reaching exactly 0% or 100% at any finite gap (verified: still nonzero — `1e-250` — at `gap=1000`) — "never truly impossible, however small" holds by construction, no floor/ceiling clamp needed.
  - **Why not modulate per-tick scoring rates by gap instead (letting the outcome emerge from score, as the old model did)?** Tried first, and rejected after simulation: compounding even a modest per-tick bias over 90 independent ticks amplifies far past the intended per-match sensitivity (a naive "scale conversion rate by `10^(-gap/4)` every tick" made a mere 1-level gap already decide ~82% of matches, nowhere near the target curve), and no clean closed form maps a per-tick constant to a target match-level curve — every value tried fit one gap and drifted at others. Resolving the outcome directly, once, via the exact formula is both simpler and exactly correct by construction, which is what "simulate and adjust if needed" surfaced as the right call here.
- The remaining `1 - pWin` splits into draw/loss via `pDraw = 0.5 × closeness × (1 - pWin)`, `closeness = 4 × pWin × (1 - pWin)` (peaks at 1 exactly at `gap=0`, tapers to 0 at extreme mismatches in either direction) — draws are commonest in close matches and rare in blowouts, matching how real sports-analytics models layer draw probability on top of (rather than derive it by simply halving) a binary win-expectancy curve. At `gap=0`: 50% win / 25% draw / 25% loss. Simulated against the real `tick()` path (not just the formula in isolation): emergent long-run rates track the target curve within ~1.3 percentage points at every gap tested (`-6` to `+6`).
- The resolved outcome (plus the drawn `opponentLevel`) is stored ONCE in `SoccerMatchState` (`resolvedOutcome`/`opponentLevel`, both `undefined` until the first tick) and just read back out by `getOutcome()`/`getPerformanceFactor()` for the rest of the match — this is what keeps those two pure, idempotent functions of `state` alone: the live "if the match ended now" `VentureCard.tsx` preview calls them every render, and must never re-roll a different answer on repeated calls against the same state. `getOutcome()` no longer takes a `context` parameter at all (soccer's own implementation doesn't need it there — `types.ts`'s interface still declares it optionally, for a hypothetical sport module that resolves differently). `VentureCard.tsx` also now shows "Facing a Level N opponent" once `tier.match.opponentLevel` is defined.
- ~~**Score/outcome coherence — a deliberate, documented tradeoff, not an oversight:** the tick-by-tick home/away chance-then-conversion scoring (same formula as step 2, completely unchanged) is now purely decorative "flavor" for the live score ticker — it does not decide the payout anymore. On the match's FINAL tick only, the score gets minimally nudged (only if it doesn't already agree) so it matches `resolvedOutcome`'s sign by the time the match completes. For most of a match's 90 ticks, the live score and the "if the match ended now" line can occasionally look inconsistent (e.g. the line already says "LOSS" while the flavor score currently has the player narrowly ahead by chance) — accepted rather than engineered away, because (a) a completed match's final score is never actually shown next to its outcome badge anywhere in this UI regardless (the match resets to 0-0 the instant it completes, true since well before this session), and (b) a full "reveal a predetermined score's goals at plausible-looking moments across the match" system was judged not worth the added complexity for a purely cosmetic gap.~~ **Revisited, not just re-accepted** — real playtesting surfaced exactly the disconnect this bullet predicted, badly enough that it read as a bug. See the fifteenth amendment below: the score now gradually trends toward the resolved outcome across the whole match via a growing bias, not just a late-tick nudge.
- **`getPerformanceFactor` consistency — this exact class of bug bit this game for real last session (see the thirteenth amendment's "Adversarial review and fixes"), so it was designed against from the start this time:** a new `resolvedDifferential()` helper forces the goal-differential's SIGN to agree with `resolvedOutcome` even mid-match, before the final-tick nudge has had a chance to actually align the flavor score — without it, a mid-match preview could show "LOSS" (from `resolvedOutcome`) while the flavor score currently has the player ahead by chance, letting a decisive-win-shaped margin bonus leak into a payout that's actually a loss/draw. `getOutcome()` and `getPerformanceFactor()` share one resolution path (`resolvedOutcomeOf()`/`resolvedDifferential()`) so they're structurally unable to drift apart, rather than relying on two independently-written functions staying in sync by convention.
- **Playtest across gap scenarios:** simulated match sequences at `gap=-6` (heavy favorite, ~97% win — long win streaks are common, occasional losses/draws still occur), `gap=0` (close matchup — a believable mixed W/D/L sequence), and `gap=+6` (heavy underdog, ~3% win — losses dominate, but a win is never literally impossible). Feels right: a freshly-unlocked tier is a genuine uphill climb without being a brick wall, and a well-trained tier stays mostly-but-not-always winning — the "however small, never zero" requirement is felt, not just mathematically true.

**Adversarial review and fixes.** A workflow-based review (5 lenses × adversarial verification, 19 agents) specifically hunting for match-resolution bugs and interaction regressions raised 7 candidate findings and confirmed 6; the following were fixed:
- **Save-migration bug (medium), the same class of issue this codebase has been bitten by before:** the once-per-match resolution step was gated only on `resolvedOutcome === undefined`, not `tickIndex === 0`. A tier already mid-match (`tickIndex` 1-89) in a save persisted from BEFORE this session — a near-certainty for this repo's own extensively-playtested save — has a `match` object under the OLD schema with no `opponentLevel`/`resolvedOutcome` keys at all, which looks identical to "this match's first tick." That would silently re-roll a fresh, score-disconnected outcome partway through an in-progress match, and the final-tick nudge would then forcibly rewrite whatever lead the player had already built up to match it (verified: a persisted 2-0 home lead at tick 50 resolved to something other than 'win' in 93% of trials, then got its visible score overwritten to match). Fixed by requiring `tickIndex === 0` too — a legacy mid-match match now simply never resolves via the new model for the rest of its own lifetime, falling back to comparing its real final score exactly as the pre-this-session code did; only the NEXT match (a genuine fresh start) uses the probability model.
- **Notification crash (high):** `merge()`'s blind `{...currentState, ...persistedState}` spread would copy a hand-edited, non-array `notifications` value (e.g. `null`) straight into the live store, and `NotificationToasts.tsx`'s unconditional `.length` check would then throw on the very first render — a full white-screen crash from one malformed field in save data this app has never claimed to validate. A related issue (medium): a stale persisted toast with a non-numeric `id` would get resurrected on load and then permanently poison the `Math.max(...)+1` id generator (`NaN !== NaN` means it can never be dismissed, and every later crossing inherits the same broken id). Both fixed by the same one-line change: `merge()` now always keeps `currentState.notifications` (fresh, empty) and never trusts a persisted value for that field at all — consistent with `notifications` already being deliberately excluded from `partialize`. Verified directly in the browser: hand-injected both `notifications: null` and a stale NaN-id entry into `localStorage`; the app loads cleanly with no console errors and an empty toast list either way.
- **Milestone progress bar pre-filled after the first crossing (low):** `milestoneProgressPercent` used raw `level / nextMilestone`, which only looks right for the very first milestone (nothing earlier to net out against) — landing on level 13 read as "59% of the way to 22" despite zero training purchased since crossing 13. Fixed with a new `previousMilestoneLevel(level)` helper (the largest already-crossed milestone, or 1 if none) so progress is measured since the last crossing, not from zero: `(level - previousMilestone) / (nextMilestone - previousMilestone)`. Verified: the bar now reads exactly 0% immediately after any crossing.
- **Stale dialog text (medium, two lenses independently flagged the same root cause):** `LegacyPanel.tsx`'s "Reset for Legacy" confirmation still described the just-deleted `minWinLevel` hard-gate ("...wins may resolve as draws again until you retrain past their minimum win level") — factually wrong about how the game now works, shown immediately before an irreversible action. `LegacyPanel.tsx` wasn't touched by this session's main diff, so it never got updated to match. Fixed the sentence to describe the actual mechanic (odds against a tier's opponents drop back down post-reset, not a hard gate).
- One additional candidate (whether milestone toasts should be cleared by `resetProgress()`/`resetForLegacy()`) was correctly refuted on verification: those resets don't touch `notifications` at all, but a toast referencing a just-reset tier is at most a few seconds of harmless stale text before its own timer clears it — not a functional bug.

### Outcome-display consolidation, gradual score bias, and schema versioning (fifteenth amendment to step 2)
Three related fixes prompted by real observed play: two outcome-display bugs (one genuinely a stale tradeoff, one a previously-undiagnosed root cause) and a standing convention to stop this project's recurring pattern of ad-hoc save-migration patches.

**Root-cause investigation — TWO different bugs, not one.** Two symptoms were reported: (Case A) the score showed the player leading 1-0 at minute 51, but the "if match ended now" line said LOSS the whole match (match ultimately resolved LOSS — the line was right, the score was disconnected from it). (Case B) at minute 73, the match-status pill said LOSS while the projection line, in the SAME frame, said DRAW (match ultimately resolved DRAW — the line was right, the pill was wrong). These are NOT the same root cause:
- Case A is the thirteenth/fourteenth amendments' own documented "score is cosmetic flavor, only nudged at the final tick" tradeoff, working exactly as designed but feeling wrong in practice — a real bug in the sense that a documented tradeoff turned out to be a bad tradeoff once actually played, not a logic error.
- Case B is a genuinely different, previously-undiagnosed bug: `VentureCard.tsx`'s live pill (next to the score) was sourced from `tier.lastOutcome` — the PREVIOUS completed match's cached result, set once in `useGameStore.ts`'s `tickTier()` completion branch — while the projection line was sourced from `getOutcome(tier.match)`, the CURRENT match's already-resolved outcome. These are two different values about two different matches, both individually correct for what they represent, displayed adjacent to each other with nothing distinguishing them — reading as an inconsistency to a player watching the live score, even though neither value was actually wrong. Confirmed by tracing exactly when each updates: `lastOutcome` only changes at the PREVIOUS match's completion (inside the same `set()` that resets `match` to a fresh 0-0 state for the new one); the new match's own `resolvedOutcome` is then decided on ITS first tick and stays fixed for its whole duration. A tier that hasn't completed a match in the last 73+ ticks will show whatever `lastOutcome` happened to be from however long ago that was, right next to a projection line describing the CURRENT match — exactly Case B.

**Fix 1 — consolidate to one authoritative source per concept, not one variable.** `VentureCard.tsx` now computes `currentOutcome = getOutcome(tier.match)` ONCE and uses it for BOTH the live pill in the score row AND the projection line — the two can no longer disagree, since they're now literally the same read. `tier.lastOutcome` (the historical "how did the last completed match go" stat — still useful, still lifetime-tracked, unchanged in the store) moved to a new, clearly-labeled "Last Result" entry in the stats row (alongside Level/Matches/Lifetime), physically separated from the live score/clock/pill grouping instead of sitting inside it. Both share the same badge styling (renamed `.venture-card__outcome-badge`, was `.venture-card__last-result` — the old name only made sense for the historical one; reusing it for the live one would have been the exact naming-implies-meaning mismatch that helped cause this bug) so the visual language stays consistent while the DATA sources and locations are now unambiguous.

**Fix 2 — the score now gradually trends toward the resolved outcome, not just a late correction.** `soccerModule.ts`'s `tick()` still runs the same chance-then-conversion "flavor" scoring as before (it still doesn't decide the payout — the win-probability formula from the fourteenth amendment, `P(win) = 1/(1+10^(gap/4))`, `resolveMatchOutcome`, `opponentLevelRangeForTier`, is completely unchanged), but the rates themselves are now biased toward whichever side the ALREADY-RESOLVED outcome favors, growing linearly across the match (`progress = tickIndex/(ticksPerMatch-1)`, `strength = SCORE_BIAS_STRENGTH × progress`, `SCORE_BIAS_STRENGTH = 1.2`). For 'win'/'loss' this is a FIXED direction for the whole match (a resolved loss increasingly favors the opponent from tick 1 onward, not just near the end); for 'draw' there's no fixed side to favor, so it dynamically favors whichever side is CURRENTLY behind at the start of each tick, trending toward convergence rather than toward either team.
- **Biasing conversion rate alone wasn't enough — simulated before committing to the final approach:** with only `conversionRate` biased (leaving `chancePerTick` fixed), 25-70% of matches still needed the final-tick safety-net nudge even at strong bias values, because the underlying scoring process is too infrequent (~7 scoring chances per match per side) for a finishing-only bias to reliably overcome variance — a boosted conversion rate does nothing if the side never gets a chance to convert. Biasing BOTH `chancePerTick` AND `conversionRate` together (both clamped to `[0.02, 0.95]`) brought the nudge-needed rate down to ~1-5% for 'win'/'loss' at strength 1.2. A 'draw's exact-equality target is a harder statistical ask by nature and still needs the nudge ~36% of the time — but only ever by a single goal, a plausible-looking late equalizer, not a jarring rewrite.
- The final-tick nudge from the fourteenth amendment still exists, unchanged, as a safety net for the residual cases — Block 1's ask was "not JUST a late-tick nudge," not "no nudge at all"; removing it entirely would reopen the exact outcome-vs-score mismatch this fix targets for whatever fraction of matches the gradual bias doesn't fully resolve on its own.
- Verified live in the browser (not just simulated): pushed a fresh match to completion and watched the score actually move in the resolved direction as ticks advanced (e.g. 2-0 home by minute 69 for a resolved WIN), with the live pill and projection line agreeing throughout, exactly as designed.

**Block 3 — direct re-verification of `getPerformanceFactor` consistency under the CURRENT system (not the old minWinLevel system the original exploit fix was built against).** The thirteenth amendment's bonus-leak bug, and the fourteenth amendment's own fix for it (`resolvedOutcomeOf()`/`resolvedDifferential()` sharing one resolution path so `getOutcome()` and `getPerformanceFactor()` structurally cannot drift apart), predate BOTH this session's score-bias mechanism above. Since match resolution has now changed twice since that fix was written, it was directly re-tested rather than re-argued from the existing code's own comments: a harness imported the real, current `soccerModule.ts`/`economy.ts` and ran 1,350 full matches sweeping gaps from -10 (heavy favorite) to +10 (heavy underdog), checking `getOutcome()`/`getPerformanceFactor()` sign-consistency and the actual `calculateMatchRevenue()` payout at EVERY one of the 90 ticks of every match (121,500 individual observations total), plus 300 back-to-back matches on one tier checking for cross-match state drift/leakage. **Result: zero inconsistencies, zero overpay violations, found.** The shared-resolution-path fix from two sessions ago is robust to the mechanism underneath it changing again — it operates purely on `resolvedOutcome` vs. the score's sign, agnostic to WHY the score is what it is, which is exactly why it survived this session's score-bias change with no code changes needed to `resolvedDifferential()` itself.

**Schema versioning (new standing convention, not a one-off fix).** This project has now hit three separate save-compatibility bugs across past sessions — a persisted `tiers` array silently truncating the fresh one on load (ninth amendment), a hidden-tier-reveal exploit via hand-edited flags (also ninth amendment), and mid-match outcomes silently re-rolling under an old match schema (fourteenth amendment) — each patched ad hoc, after the fact, because no past shape change was ever versioned in the first place. Going forward:
- `useGameStore.ts` gained `CURRENT_SCHEMA_VERSION = 1` and a `schemaVersion` field on `GameState` itself (persisted, mirroring zustand's own outer `version` option so anyone inspecting a save's `state` blob directly can see its schema without knowing zustand's `{state, version}` envelope convention).
- **The standing rule:** any future change to what `partialize` persists — including a shape change nested inside something already persisted (a `VentureTier`, a `SoccerMatchState`, etc.) — MUST bump `CURRENT_SCHEMA_VERSION` by exactly 1 and add a new entry to `SCHEMA_MIGRATIONS` transforming the PREVIOUS version's shape into the new one. `migrateGameState()` applies every needed step in a loop from whatever version a save reports up to current — a save several versions behind walks through every intermediate step, never assumed to be only one version stale.
- **Not every compatibility gap needs a migration, though.** The mid-match `resolvedOutcome` gap (fourteenth amendment) deliberately did NOT get a migration step — there's no sensible backfilled value migration could write for an in-progress match's resolved outcome, so a tolerant/defensive read at the point of use (gating resolution on `tickIndex === 0`, not just "field absent") is the correct fix there, not a data transformation here. The one gap that genuinely IS a data transformation — a `tiers` array shorter than the current ladder — moved from its old home inside `merge()` into `SCHEMA_MIGRATIONS[0]`.
- **A real gotcha, caught before it shipped:** zustand's own version-mismatch check only fires when the persisted blob has a NUMERIC `version` that differs from the configured one (`typeof storedVersion === 'number' && storedVersion !== options.version`) — a blob with NO `version` field at all (every single real save this project has ever produced, since versioning never existed before this amendment) fails that `typeof` check and silently skips `migrate()` entirely, using the old shape as-is forever. A custom `createVersionBackfillingStorage()` wraps the default `createJSONStorage(() => localStorage)` to inject `version: 0` on read whenever the stored blob has no version field, so zustand's own check then correctly sees `0 !== 1` and invokes `migrateGameState`.
- **Tested against a real save shape, not just a synthetic one:** a harness built a representative pre-versioning, 6-tier save (matching what this project's own saves looked like before the ninth amendment added tiers 7-11) with real progress data — levels, in-flight match state, cumulative revenue, lifetime wins, earned achievements — fed it through the actual storage/migration/merge pipeline, and confirmed: the tiers array pads to 11 while every existing tier's progress (including a genuinely in-flight match) is preserved byte-for-byte; `schemaVersion` and the outer envelope's `version` both land on 1; the re-persisted blob carries `version: 1` so a second load doesn't re-migrate; an already-current save loads unchanged (idempotent); a brand-new save with no localStorage entry at all still starts at the current version. Also verified directly against this repo's own actual leftover browser save (not a synthetic one) — it migrated live with zero console errors, tiers padded to 11, version stamped to 1.

**Adversarial review and fixes.** A workflow-based review (5 lenses × adversarial verification, 13 agents) specifically hunting for interactions between this session's three changes above raised 4 candidate findings; all 4 survived adversarial verification (two lenses independently converged on the same root-cause bug, so this is 3 distinct real issues, all fixed):
- **High severity — Fix 2's gradual score bias silently inflated win-revenue by ~28%, contradicting its own "purely cosmetic" framing.** `getPerformanceFactor()`'s pre-existing `resolvedDifferential()` read the SAME `homeScore`/`awayScore` fields Fix 2's bias now manipulates — `resolvedDifferential()` only forces the differential's SIGN to agree with `resolvedOutcome`, never its magnitude. Because the bias funnels a resolved win toward blowout scorelines for the ENTIRE match (not just late), the "genuinely rare blowout tail" the thirteenth amendment calibrated `MAX_MEANINGFUL_GOAL_DIFFERENTIAL` against (documented as `P(|diff|>=5) ~= 3.2%`) became the majority case for a win instead (~67-68%, confirmed independently by 4 separate agent simulations against the real shipped code) — inflating the average win completion-bonus multiplier from ~1.19x to ~1.52x. This is exactly the "getPerformanceFactor must derive from the same resolution path as getOutcome" lesson from the 13th/14th amendments, reopened via a new gap: the sign half was correctly re-applied, but nothing constrained the biased score's magnitude. **Fix:** decoupled the two concerns. `SoccerMatchState` gained `resolvedMargin?: number`, drawn ONCE at resolution (alongside `resolvedOutcome`/`opponentLevel`, tick 0 only) by a new `drawResolvedMargin()` — it re-simulates the ORIGINAL, unbiased chance-then-conversion process (up to `MARGIN_DRAW_MAX_ATTEMPTS = 50` attempts) until a trial's sign matches `resolvedOutcome`, and uses that trial's differential as the economic margin. `getPerformanceFactor()` now reads `state.resolvedMargin` (falling back to the live raw differential only when it's undefined — the pre-resolution-model "no such concept" case) instead of calling `resolvedDifferential()`, which was deleted as dead code. The cosmetic `homeScore`/`awayScore` stay biased for display exactly as Fix 2 designed; the economic margin is now drawn from a statistically-independent, unbiased process that only agrees with `resolvedOutcome` on sign — restoring the original blowout-rarity calibration while leaving the visible score-trending behavior untouched. Re-verified: average win multiplier back to ~1.26x (was ~1.52x), blowout rate back to ~5.5% (was ~67%), 81,000 sign-consistency observations with zero violations, and the cosmetic trend still holds (94.9% of losses trending unfavorable by the 80%-mark at a heavy-underdog gap).
- **Medium severity — a freshly-reset match (right after every single completion, and every fresh unlock) briefly-to-indefinitely showed a false live "DRAW" badge and a flat-draw payout projection.** `getOutcome()`'s documented fallback for an unresolved match (`resolvedOutcome` undefined) is a 0-0 tie-break, correctly returning `'draw'` — a sensible fallback value, but Fix 1's consolidation newly routed that same fallback into the prominent, score-adjacent live badge (previously that slot showed `tier.lastOutcome`, the real previous result, which stayed correct until overwritten by the NEXT completion). Every match reset (`tickTier()`'s completion branch calls `soccerModule.createInitialState()`) and every `unlockTier()` on a brand-new tier hit this, persisting until the tier's next real tick — instant while manually clicking, but up to ~17.4s at tier 10 for a manager-hired tier, or indefinite for an unmanaged one. **Fix:** `VentureCard.tsx` now gates both the live outcome badge and the projection line on `matchStarted = tier.match.elapsedTicks > 0`, hiding them entirely (same pattern as the pre-existing opponent-level line) until the match has had at least one real tick — `getOutcome()`/`getPerformanceFactor()` themselves are unchanged, this is purely a display-layer gate. Verified live in the browser: a freshly-reset tier shows "0 - 0" with no badge and no projection line; one click later, the badge and projection appear correctly alongside the newly-drawn opponent level.
- **High severity — a hand-edited, non-array persisted `tiers` (e.g. `tiers: null`) crashed the entire app on load, where the pre-session code silently discarded it instead.** Block 2's `SCHEMA_MIGRATIONS[0]` guards its array-padding logic with `Array.isArray(state?.tiers)` — for `tiers: null` this is false, so the step returns the corrupted value completely unchanged instead of repairing it, and the old unguarded `merge()` (which used to throw synchronously on `merged.tiers.length` for a non-array, a throw zustand's own persist internals swallow in a trailing `.catch()` before `set()` is ever called — an accidental protection, not a designed one) no longer has any tiers-shape check left at all. The corrupted value now sails through `merge()`/`set()` cleanly and crashes on the very next render (`useMatchTicker`'s unconditional `s.tiers.filter(...)`, called before any route, with no error boundary anywhere in the app) — worse than before, since it also makes the Settings page's DEV-wipe recovery button unreachable. Reachable at BOTH schema versions: a version-0 (unversioned) corrupted save skips repair via the `Array.isArray` guard above; a save that already reports the current version skips `migrate()` entirely (zustand only invokes it on a version mismatch), so the corrupted value never even reaches that guard. **Fix:** `merge()` now explicitly validates `Array.isArray(merged.tiers) && merged.tiers.length === SOCCER_VENTURE_TIERS.length` and falls back to `currentState.tiers` (the fresh in-memory defaults) if not — restoring the original silently-keep-defaults behavior explicitly, rather than depending on an accidental throw deep in a promise chain to keep providing it. Verified: a `tiers: null` save at version 0, a `tiers: null` save already at the current version, and a wrong-length (but valid-array) `tiers` all load cleanly with a full fresh 11-tier ladder and no crash; a genuinely valid, correctly-shaped save still loads its real per-tier data unchanged (the guard isn't overly aggressive).
- No findings were rejected on verification this time — all 4 candidates (3 distinct bugs) held up.

### Delayed outcome-word reveal (sixteenth amendment to step 2 — display-timing only, no resolution-logic change)
Requested: hide the explicit outcome WORD (WIN/DRAW/LOSS) — wherever it appears during an in-progress match — until the match has actually completed, while keeping the projected payout NUMBER visible throughout (it has real decision value when managing several auto-playing tiers at once). Explicitly NOT a resolution-logic change: the underlying result is still decided once, at kickoff, by the unchanged win-probability model (`resolveMatchOutcome`/`opponentLevelRangeForTier`, fourteenth amendment) — this only changes WHEN the already-decided word is displayed.
- `VentureCard.tsx` gained `matchComplete = tier.match.elapsedTicks >= DEFAULT_SOCCER_CONFIG.ticksPerMatch`, alongside the existing `matchStarted = tier.match.elapsedTicks > 0`. Both the score-row badge and the projection line now gate the WORD on `matchComplete` — never a second/divergent computation, both still read the same single `currentOutcome`/`currentPerformanceFactor` values this file has used since the fifteenth amendment's consolidation fix, per this session's explicit instruction not to reintroduce that class of bug.
- The projection line now has three states, all reading from that one source: not started (`matchStarted` false) shows "Projected payout: N/A — kickoff pending" rather than a possibly-misleading number computed from the pre-resolution fallback outcome; in progress (`matchStarted && !matchComplete`) shows "Projected payout: +N Revenue" — number only, no word; complete (`matchComplete`) shows the unchanged "If the match ended now: OUTCOME, +N Revenue".
- **A genuine architectural wrinkle, investigated and documented rather than silently worked around:** `useGameStore.ts`'s `tickTier()` resets a completed match to `soccerModule.createInitialState()` in the SAME `set()` call that finalizes it — the finishing tick's own `elapsedTicks === ticksPerMatch` state (`nextMatch` in that function) is used only transiently to compute the outcome/payout via `finalizeMatch()`, and is never itself written into the store. This means a rendered frame with `tier.match.elapsedTicks === ticksPerMatch` essentially never occurs in live play — a tier's visible `elapsedTicks` jumps straight from `ticksPerMatch - 1` to a fresh `0`. `matchComplete` is still computed correctly (not hardcoded false) so the word would display correctly if this ever changes, but its practical effect today is "the live word never shows during a match" — the just-finished result remains visible via the separate, always-on "Last Result" stat (`tier.lastOutcome`, unaffected by this change, per the fifteenth amendment's consolidation). This is the correct, intended behavior given the request (word suppressed until complete; the completion reveal already happens via the existing Last Result stat) — not a compromise.
- Because that specific frame is unreachable through normal play, the "at the moment of completion" test case was verified directly against the real `soccerModule.tick()`/`getOutcome()`/`getPerformanceFactor()`/`calculateMatchRevenue()` output for a match's actual final tick (i.e. the same `nextMatch` value the store computes and discards), confirming the word and number would display correctly and consistently if that frame were ever rendered — not a fabricated scenario, the literal transient value the store already produces internally.
- **Verified live in the browser**, not just via harness: a fresh/just-reset match shows "Projected payout: N/A — kickoff pending" with no badge; mid-flight at multiple checkpoints (manually and via an auto-playing manager) shows the number updating with no word; a completed match instantly resets to the fresh "N/A — kickoff pending" state while the separate "Last Result" stat correctly shows the finished outcome. The training-milestone toast (fourteenth amendment) still fires correctly and on time — confirmed via a `MutationObserver` (the toast's 4-second dismiss window was too short to reliably catch via manual polling given tool round-trip latency) that it's added to the DOM the instant a milestone level is crossed. Multi-tier independent auto-play (thirteenth amendment) is unaffected — two managed tiers observed auto-advancing at their own independent paces, each correctly showing its own suppressed-word/visible-number display.

### Outcome-independent expected payout, generalized prestige gating, and the Testing Conventions section (seventeenth amendment to step 2)
Three changes with deliberately different verification depth, per the new "Testing Conventions" section this session also added (see below) — this session is itself the first application of that rule, not just its documentation.

**Block 1 — the in-progress "projected payout" number could leak the resolved outcome even with the word hidden (moderate risk, full verification).** The sixteenth amendment hid the outcome WORD during an in-progress match, but the payout NUMBER shown next to it was still derived from `currentOutcome`/`currentPerformanceFactor` — this match's ALREADY-RESOLVED (just hidden) result. A sharp player could infer the real result from the number alone (e.g. it jumping to the flat loss payout the instant the match starts, staying flat all match, then confirmed by "Last Result" at completion).
- **Fix — a genuine, outcome-independent expected value, not a reworded resolved number.** `soccerModule.ts`'s `resolveMatchOutcome()` had its probability formula (`gap`/`pWin`/`closeness`/`pDraw`, unchanged) extracted into a new pure, exported `matchOutcomeProbabilities(playerLevel, opponentLevel): Record<MatchOutcome, number>` — `resolveMatchOutcome` now calls this internally instead of duplicating the math, so the real resolution path and the new preview are structurally unable to drift apart (the same "one authoritative source" fix pattern used repeatedly elsewhere in this file). `economy.ts` gained `expectedMatchRevenue(probabilities)`, summing `probability[outcome] * calculateMatchRevenue(outcome, 0.5)` across win/draw/loss — reusing the real `calculateMatchRevenue` directly (never reimplementing `REVENUE_BY_OUTCOME` or the margin-bonus formula). `VentureCard.tsx`'s in-progress display now computes `expectedPayout` from ONLY `tier.level` (live) and `tier.match.opponentLevel` (this match's own already-drawn, already-displayed-via-"Facing a Level N opponent" value) — never touching `resolvedOutcome`/`resolvedMargin`/the live score — so by construction the number cannot correlate with what the match actually rolls. Labeled "Expected payout: ~N Revenue" (not "Projected") to read honestly as an estimate. Pre-tick ("N/A — kickoff pending") and at-completion (word + real resolved payout) states are unchanged from the sixteenth amendment, per instruction.
- **Neutral-factor honesty, documented rather than glossed over:** using performance factor 0.5 for all three outcome terms is mathematically EXACT for draw and loss (this file's own decisiveness formula clamps to 0 at or below neutral, so neither ever receives a margin bonus at any factor), but a deliberate, honest LOWER BOUND for win specifically — a win's true average payout includes a real margin-bonus premium, but computing that premium's expectation would require either a second, separately-maintained statistical model of the margin distribution (rejected — exactly the "two copies of the same math drift apart" bug class this project has repeatedly been bitten by) or live random sampling at render time (rejected — would make the number flicker between renders and break the "impossible to correlate" guarantee's determinism). Falls back to the OLD resolved-outcome-based number only when `tier.match.opponentLevel` is undefined — a legacy pre-migration mid-match state that, per the fourteenth amendment, never got a `resolvedOutcome` either, so there is no hidden result to leak in that one case.
- **Verified — real scrutiny, per this session's own risk calibration:** a direct-import harness (9 checks) confirmed the probability formula against documented anchors (gap=0 → exactly 50/25/25; gap=+3 → exactly the ~15.1% anchor), confirmed the real `tick()`-driven resolution still matches those probabilities empirically (20,000 matches, within 2pp), confirmed `expectedMatchRevenue` exactly matches a manual sum using the real `calculateMatchRevenue` (not a coincidental reimplementation), and — the key proof — ran 3,000 matches at a fixed (level, opponentLevel) and recorded the expected-payout value at every tick of every match, split by that match's ACTUAL resolved outcome: **all 270,000 observations, across all three outcome categories, produced the exact same single number**, direct empirical proof of zero correlation rather than an argument from code inspection alone. Also confirmed the number is a real blend (not a disguised passthrough — a heavy underdog's expected payout sits well below the flat win rate and above the flat loss rate), stays within sane bounds at every gap tested, and correctly rises immediately when a mid-match "Improve Training" purchase raises the live level (real decision value, not a frozen snapshot). A follow-up independent agent review (asked to specifically hunt for any leak path, formula drift, or fallback that could expose a hidden outcome) found no issues. Verified live in the browser: mid-match number stayed flat across an entire match regardless of what it ultimately resolved to, updated instantly when leveling up mid-match, and a fresh redraw after completion recomputed cleanly for the new match's own opponent.

**Block 2 — "Reset for Legacy" only checked one fixed tier, letting a player skip every tier in between at ANY prestige stage (low-moderate risk, targeted harness).** The gate only ever checked whether `FIRST_PRESTIGE_TRIGGER_TIER_ID` ('world-championship') was unlocked — at every prestige stage, forever. Since tiers can be unlocked in any order (`unlockTier()` only checks that ONE tier's own cost, never "the previous tier must be unlocked first"), a player could hoard Revenue on Local Game alone and unlock World Championship directly, skipping tiers 1-4 entirely, and still prestige. Worse, after a first prestige reveals a new hidden tier (e.g. legends-circuit), the gate kept checking the SAME fixed world-championship tier forever — a multi-prestige player could permanently skip unlocking every post-prestige tier and still reset indefinitely.
- **Fix:** new exported `allVisibleTiersUnlocked(tiers, prestigeCount)` (`soccerModule.ts`) checks that EVERY tier in the currently-visible prefix (`tiers.slice(0, revealedTierCount(prestigeCount))`) is unlocked — generalizing across every prestige stage by construction (the same call, given a larger `prestigeCount`, checks a larger visible set) rather than by special-casing "first prestige" vs. "later ones." Takes a plain `{unlocked: boolean}[]` rather than importing `VentureTier` from the store, keeping the dependency direction store → soccerModule, never reversed. `useGameStore.ts`'s `resetForLegacy()` and `LegacyPanel.tsx`'s locked/available UI split both now call this exact same function — not two independently-written conditions — so the panel's displayed state can never drift from what the store would actually accept (the same "one authoritative source" lesson Block 1 also applied). `LegacyPanel.tsx`'s locked-explainer copy changed from naming one fixed tier ("Reach and unlock The World Crown...") to naming the actual current last-visible tier dynamically ("Unlock every tier through {lastVisibleTier.name}...") — correct at every stage, not just the first.
- **Verified with a targeted harness** (proportionate to this block's lower risk, per the Testing Conventions below — not a full adversarial workflow): confirmed a fresh game correctly can't prestige; confirmed the exact bug scenario (world-championship unlocked, an earlier tier deliberately left locked) now correctly blocks the reset, then succeeds once that tier is also unlocked; confirmed the SAME pattern holds after a first prestige (the newly-revealed tier must also be unlocked, not just the original six); fast-forwarded through all 5 post-prestige reveal stages to saturation (11 of 11 tiers visible) and confirmed a 6th prestige attempt still correctly requires all 11 unlocked, not just world-championship; confirmed legacyPoints/hasPrestiged/tier-reset semantics are unchanged once the gate passes — this fix only changes the CONDITION, not what happens after it's met.

**Testing Conventions (new, documentation-only — see the dedicated section below).** Formalizes a rule this session explicitly applied to itself: verification depth scales with risk (cosmetic changes get build+lint+browser confirmation; store/economy/match-resolution/prestige/persistence changes get the full harness-plus-adversarial-review treatment), based on this project's own history of exactly which areas have produced real, shipped bugs. Intended to guide future sessions' own judgment without needing to be re-specified in every prompt.

- Verified: build/lint clean across all three blocks; no regressions in any of the existing scratch-harness regression suites from prior sessions (margin decoupling, outcome-word timing, schema migration, the fifteenth amendment's three adversarial fixes) re-run after this session's changes.

### Baseball: second sport as a plugged-in module (step 3 — validates the engine abstraction)
The first genuinely new build-order step since step 2 shipped. Two parts: an architecture audit (required before writing any baseball code, per instruction) and then baseball itself — a deliberately small 3-tier validation slice, not the full arc.

**Part 0 — architecture audit result.** Confirmed BEFORE writing any baseball code, exactly as instructed: `matchOutcomeProbabilities`/`resolveMatchOutcome` (the win-probability formula) were embedded specifically inside `soccerModule.ts`, not sport-agnostic by location — but sport-agnostic by NATURE (the formula only ever consumes two numeric levels, nothing soccer-specific). `expectedMatchRevenue`/`calculateMatchRevenue` (`economy.ts`) were ALREADY sport-agnostic — no change needed, baseball calls them directly. The "cosmetic score biases toward the fixed outcome across the match" mechanism is genuinely soccer-specific by NATURE, not just by location — it operates on soccer's own "chance-then-conversion per tick" scoring model, which has no direct analog in baseball's at-bat resolution; per this project's own engine/sport-module split principle ("sport-specific rules live in plugged-in sport modules"), baseball needed its OWN analogous mechanism following the same architectural PATTERN (resolve the real outcome once, then bias a cosmetic per-tick flavor simulation toward it), not literally shared code.
- **Fix, done before any baseball code:** hoisted the win-probability formula into a new `src/engine/winProbability.ts` (`matchOutcomeProbabilities`, `resolveMatchOutcome`, plus new `matchOutcomeProbabilitiesWithoutDraw`/`resolveMatchOutcomeWithoutDraw` for the no-draw decision below) — `soccerModule.ts` now imports and calls these instead of defining them locally, so the actual resolution formula has exactly one copy, used by both sports.
- **Beyond the letter of the audit, applying its spirit:** while auditing, found the tier-cost/revenue formulas (`tierUpgradeCost`, `trainingEffectMultiplier`/`TRAINING_MILESTONE_LEVELS`, `tierPerTickRevenue`, `opponentLevelRangeForTier`, `autoTickIntervalMsForTier`) were ALSO soccer-housed-but-sport-agnostic-by-nature (pure functions over a generic tier config + numeric level/tierIndex, zero soccer vocabulary) — building baseball would otherwise have required copy-pasting these byte-identical, which is exactly the "second copy of the same math" bug class the audit was about, just not literally the three functions named in the request. Hoisted these too, into a new `src/engine/ventureTiers.ts`, alongside a new shared `VentureTierConfig`/`VentureTierState<TState>` type and a new `resolveVentureTierTick<TState>()` helper (composes `advanceTick`/`isMatchComplete`/`finalizeMatch` + the per-tick/completion revenue formulas into the one function every sport's `tickTier`-equivalent store action calls).

**Engine contract extension — a real, small, deliberate change, not silently papered over.** Baseball's match length is genuinely variable: a half-inning needs exactly 3 outs, which random hit/out/walk-style at-bat rolls take a variable number of ticks to reach (a quick 3-up-3-down half-inning vs. a long rally). The existing `SportModule.ticksPerMatch` (a single fixed number) cannot represent this. Investigated whether it could be worked around with a documented fixed-estimate fallback instead of a contract change — rejected: a fixed-tick "match" that keeps ticking past when the game (all innings played) is actually decided would either silently discard real game state, or require the sport module to internally freeze while still "ticking," both worse than the small contract addition:
- `SportModule<TState>` gained an OPTIONAL `isMatchComplete?(state: TState): boolean` (`engine/types.ts`). Soccer doesn't define one — completely unaffected, byte-for-byte the same `tickIndexAfterTick >= ticksPerMatch` check as before. `tickEngine.ts`'s `isMatchComplete(module, tickIndexAfterTick, state)` gained a required third `state` parameter (the one call site — now inside `ventureTiers.ts`'s `resolveVentureTierTick` — updated accordingly) and consults `module.isMatchComplete(state)` when present, falling back to the original check otherwise. Baseball's own `isMatchComplete` is a one-liner: `state.gameOver === true`, with `gameOver` set exactly once, at the tick that actually decides the game.
- Also added `MatchContext.matchLength?: number` — a generic, optional per-match length parameter (mirroring the existing `level`/`opponentLevelRange` fields' own genericity), for a sport whose match STRUCTURE (not just its win probability) varies by tier. Soccer ignores it entirely; baseball reads it once at tick 0 (`context.matchLength`, the tier's configured inning count) and stores it into its own opaque state as `totalInnings`, matching the "resolve once into state, read back out" pattern this file uses throughout.
- `ticksPerMatch` itself stays a required field even for baseball (`BASEBALL_ESTIMATED_TICKS_PER_MATCH = 52`, an ESTIMATE derived from expected at-bats per half-inning) — used only to size VentureCard's progress bar, explicitly documented as non-authoritative; `isMatchComplete` is what actually ends a baseball match.

**No-draw resolution — a real design decision, not a default.** Real baseball has no ties (extra innings until decided), but this project's shared economy/achievements/milestone systems assume a generic win/draw/loss `MatchOutcome` triad throughout. Two options considered: (a) an abstracted draw for triad-consistency with soccer (zero special-casing, but unrealistic for the one sport-specific rule explicitly flagged as real), or (b) genuine no-draw resolution reusing the SAME win/loss ratio the shared formula produces, with its draw probability mass proportionally redistributed. Chose (b): `matchOutcomeProbabilitiesWithoutDraw`/`resolveMatchOutcomeWithoutDraw` (`winProbability.ts`) collapse `matchOutcomeProbabilities`' draw mass into win/loss proportionally (the same technique real sports-betting "no draw" markets use) — no new probability model, just a post-processing step reusing the identical underlying formula, so nothing about the ACTUAL win-probability curve is duplicated or re-derived. `MatchOutcome`'s type itself is unchanged (still `'win'|'draw'|'loss'`) and the economy/achievements pipeline needs zero changes to support this — baseball simply never emits `'draw'` in practice, an allowed degenerate case of an already-generic type. Verified directly (see below): zero draws across 900 simulated games at a wide gap sweep.
- **Tied-after-regulation handling — a deliberate simplification, documented rather than glossed over:** the LIVE/cosmetic game does not actually simulate real extra innings when tied after the scheduled length — it applies a bounded 1-run "nudge" toward the already-resolved outcome, mirroring soccer's own final-tick nudge safety net (same reasoning: a bounded tick budget for the displayed game). The SEPARATE economic-margin resimulation (`drawResolvedMargin`, decoupled from the cosmetic game exactly like soccer's, for exactly the same "don't let a cosmetic bias leak into the economic margin bonus" reason — see the thirteenth/fifteenth amendments) DOES simulate genuine extra-innings-until-decided, since an isolated resimulation used only for the economic signal can afford to actually loop.
- **Walk-off rule, implemented for authenticity (a nice touch that directly helps "does baseball feel meaningfully different"):** the home team (bats bottom, matching real baseball's home-bats-second convention) skips or ends its final at-bat the instant it's already ahead at/past the scheduled inning count — both the "already ahead going into the bottom of the last inning" and "takes the lead mid-at-bat" cases are implemented.

**Tick granularity — one tick = one at-bat, not one inning, per instruction.** `BaseballMatchState` tracks `inning`/`half`('top'|'bottom')/`outs`/`homeScore`/`awayScore` plus the same `opponentLevel`/`resolvedOutcome`/`resolvedMargin`/`elapsedTicks` shape soccer's state has. Each tick resolves ONE simplified at-bat (out / scores a run / reaches base without scoring — a deliberate simplification with NO individual baserunner/bases-state tracking, judged not worth the added complexity for this validation slice; the simplification is visible only in HOW a run gets scored, never in whether shared math gets duplicated). A cosmetic score-bias mechanism (`scoreBiasFavorsHome`, `SCORE_BIAS_STRENGTH=1.2`, growing with an innings-based `progress` measure) biases the currently-batting team's out/score-given-not-out probabilities toward the already-resolved outcome — the baseball-native analog of soccer's own mechanism, using baseball's vocabulary (out probability / score-given-not-out probability) instead of soccer's (chance per tick / conversion rate), per Part 0's finding that this piece is not literally shareable.

**Tier calibration (3-tier validation slice, real age-level innings counts).** "Tee Time" (3 innings, T-ball/rec-league), "Little League Nights" (6 innings), "Varsity Diamond" (7 innings, high school) — `BASEBALL_TIER_INNINGS = [3, 6, 7]`, a parallel array indexed like `BASEBALL_VENTURE_TIERS` (inning count is baseball-specific vocabulary, not part of the shared generic `VentureTierConfig` shape). Costs positioned to feel like a deliberate SECOND commitment layered over soccer's own ladder, not a parallel easy track: `unlockCost` 3000/15000/70000 — interleaved between soccer's regional-championship(2250)↔national-league(11250), national-league↔continental-cup(56250), and continental-cup↔world-championship(281250) respectively, continuing the same ~5x-ish compounding soccer's own ladder uses. `baseRevenueMultiplier` 20/45/130 (between soccer's regional-championship=12↔national-league=35, etc.). Tee Time itself is its own locked-card purchase — unlike soccer's `local-game`, NO baseball tier starts unlocked (`createInitialBaseballTiers()` — every tier `unlocked: false`), since entering a whole second sport is meant to read as a deliberate milestone, not a freebie.

**Store/UI genericization — what's actually shared vs. what's deliberately still parallel.** `useGameStore.ts` gained a parallel `baseballTiers: BaseballVentureTier[]` array and four baseball actions (`tickBaseballTier`/`upgradeBaseballTier`/`hireManagerForBaseballTier`/`unlockBaseballTier`), structurally parallel to soccer's four. The actual ECONOMIC resolution (the part with real bug history) is fully shared via `resolveVentureTierTick` — both sports' tick actions call the exact same function; only the thin store-side plumbing (which array to update, which currency bucket, calling the shared `applyEarnedAchievements` helper) is duplicated at the code-structure level. This is a deliberate, documented tradeoff for this validation slice: forcibly unifying the store's `set()`-call orchestration into one fully generic action in the same session that also built baseball from scratch was judged a higher regression risk to soccer's own extensively-playtested, 17-amendments-deep behavior than a small amount of parallel-but-legible plumbing — matching this project's own Testing Conventions ("calibrate per change" and "a lighter-weight way to satisfy the same don't-skip-verification principle"). `VentureCard.tsx` itself IS fully generic now (takes a `SportModule<TState>`, `VentureTierConfig`, `VentureTierState<TState>`, and callback props, with zero hardcoded soccer imports) — `SoccerVentureCard.tsx`/`BaseballVentureCard.tsx` are the thin per-sport adapters, and this genericization (not the store plumbing) is the part that most directly validates "a second sport plugs in without changing the shared component," this build-order step's own stated purpose.
- **Deliberately independent of Legacy/prestige, per instruction:** baseball tiers are NOT gated by `isTierRevealed`/`prestigeCount`, NOT included in `resetForLegacy()`'s `allVisibleTiersUnlocked` gate check, and NOT reset by a successful `resetForLegacy()` (only the full `resetProgress()` dev wipe resets `baseballTiers`) — a soccer prestige resets THIS SOCCER RUN, not a separate, parallel venture. Baseball DOES share the same global `currencies.revenue` pool, the same Legacy "Revenue Boost"/"Veteran Discount" multipliers (already documented as "every tier, every run, forever," not soccer-specific), and the same `lifetimeStats.totalWins` achievement line (already documented as "across every venture tier combined") — verified directly, not just assumed.
- **Schema version bumped 1 → 2**, per this project's own standing schema-versioning convention (fifteenth amendment): `SCHEMA_MIGRATIONS[1]` backfills a missing/wrong-length `baseballTiers` with fresh locked defaults; `merge()` gained the same never-trust-the-shape array guard for `baseballTiers` that `tiers` already had.

**Verified — full adversarial/dual-verification treatment, per this project's own Testing Conventions (this session touches match-resolution AND a new sport-agnostic hoist, squarely the high-risk case).**
- A direct-import Node harness (7 checks) confirmed: 1,500 games complete without hitting a safety cap across all 3 real tier configs; zero draws across 900 games at a wide gap sweep; empirical win rate matches `matchOutcomeProbabilitiesWithoutDraw`'s theoretical curve within 4pp; zero `getOutcome`/`getPerformanceFactor` sign inconsistencies and zero loss-overpay violations across 3,000 games (the exact bug class this project shipped twice before); average win-revenue multiplier is a modest ~1.25x (NOT the ~1.5x+ inflation this project has shipped as a real bug twice); the cosmetic score-bias trend is genuinely visible (99% of losses trending unfavorable by mid-game at a heavy-underdog gap); the walk-off rule is directly observed firing; the real economic blowout rate (via `resolvedMargin`, not the cosmetic score) is a genuine minority (~13%) of wins, not the majority.
- A second harness (7 checks) confirmed: a version-0 save migrates through BOTH schema steps correctly, gaining a fresh locked `baseballTiers`; a version-1 save (no `baseballTiers` field at all — the real shape an actual prior save has) migrates cleanly; an already-current save with real baseball progress is idempotent (not re-migrated/clobbered); a corrupted `baseballTiers` falls back to fresh defaults with no crash; unlocking and playing a baseball tier correctly feeds the shared `currencies.revenue` pool and `lifetimeStats.totalWins`; Legacy's Revenue Boost/Veteran Discount correctly apply to baseball's per-tick revenue and unlock cost; `resetForLegacy()` leaves `baseballTiers` completely untouched while `resetProgress()` correctly wipes both.
- Every pre-existing regression harness from prior sessions (margin decoupling, outcome-word timing, the fifteenth/seventeenth amendments' fixes, the prestige-gate generalization) was re-run against the post-hoist code and still passes unchanged — confirming the hoist preserved soccer's behavior byte-for-byte.
- A workflow-based adversarial review (6 lenses — engine contract, baseball resolution math, store wiring/regression risk, UI genericization, cross-sport economy integration, and the baseball state machine — × 2 independent skeptics per candidate finding, 26 agents) raised 10 candidate findings; 9 survived adversarial verification. The following were fixed:
  - **High severity, found independently by 2 lenses — VentureCard's "Expected payout" preview hardcoded soccer's WITH-draw probability formula for every sport, silently mispricing baseball.** `VentureCard.tsx` imported and called `matchOutcomeProbabilities` (soccer's formula, which allocates real probability mass to a `draw` bucket) directly, regardless of which sport it was rendering — but baseball has no draw state at all (`resolveMatchOutcomeWithoutDraw`). At a dead-even matchup this understated baseball's true expected payout by ~11% (63.75 shown vs. ~71.67 correct); at other gaps the WITH-draw formula could be too HIGH instead — the direction isn't one-size-fits-all, only the fact that it was consistently wrong. This is exactly the "generic component secretly assumes one sport's shape" failure mode the genericization work was supposed to prevent. **Fix:** `VentureCard.tsx` gained a `computeOutcomeProbabilities` prop instead of a hardcoded import — `SoccerVentureCard.tsx` passes `matchOutcomeProbabilities` directly; `BaseballVentureCard.tsx` passes a small adapter reusing `matchOutcomeProbabilitiesWithoutDraw`'s win/loss ratio with `draw: 0` — never a second, separately-maintained approximation of either. Verified: the corrected formula matches an independent manual calculation exactly at every gap tested, and the live browser's displayed number (865 Revenue for a level-1 Tee Time tier facing a level-3 opponent) matches that same manual calculation to the Revenue.
  - **High severity — `resetForLegacy()`'s own doc comment claimed baseball was untouched "in either direction," which was true for `baseballTiers`' state but false for the shared currency.** Since `currencies.revenue` is genuinely ONE pool spent/earned by both sports, `resetForLegacy()`'s existing `currencies: { revenue: startingRevenue(...) }` line (unchanged — a soccer prestige is SUPPOSED to zero out banked wealth, exactly as it always has) also silently erases any Revenue a baseball-invested player was banking toward their next tier's unlock/manager cost, even though that tier's own unlocked/level state survives untouched. **Fix (a disclosure fix, not a behavior change):** true per-sport currency attribution would require tracking which sport earned which already-pooled, already-partially-spent dollar — out of scope for a validation slice, and arguably contrary to "Revenue is ONE shared pool" being an explicit design decision. Corrected the store's doc comment to stop overclaiming full independence, and added an explicit clause to `LegacyPanel.tsx`'s reset-confirmation dialog disclosing that baseball's Revenue gets wiped too (while its tier progress doesn't) — so a player invested in both sports isn't blindsided by an irreversible action.
  - **Medium severity — one shared `ticksPerMatch` estimate (52 ticks) was only accurate for one of baseball's three tiers.** Simulated: Tee Time (3 innings) averages ~28 ticks (progress bar reads barely-half-done at the moment the match actually completes and resets); Varsity Diamond (7 innings) averages ~61 ticks (bar clamps at a false 100% for roughly the last 15% of the match's real duration). No economic impact (`isMatchComplete` was always separately authoritative), but a real, reproducible visual inconsistency more severe than "a rough estimate" plausibly implies. **Fix:** `VentureCard.tsx` gained an optional `estimatedTicksPerMatch` prop (defaults to `sportModule.ticksPerMatch` when omitted, correct for soccer's one-size-fits-all length); `baseballModule.ts` gained `estimatedTicksForBaseballTier(tierIndex)`, deriving a real per-tier estimate from the same at-bats-per-half-inning math, which `BaseballVentureCard.tsx` always supplies.
  - **Low/medium severity, found independently by 3 lenses — `tick()` could freeze a match forever if `context.opponentLevelRange` was ever omitted, even with `matchLength` supplied.** `totalInnings` was only ever assigned INSIDE the `opponentLevelRange`-gated resolution block, so omitting just that one field (which `MatchContext`'s own doc comment says every field should be independently, safely ignorable) left `totalInnings` permanently `undefined`, which then permanently skipped the entire innings/outs/score progression block — `isMatchComplete()` would return `false` forever. Currently unreachable via the real app (`tickBaseballTier` always supplies a fully-populated context), but a real contract violation and a latent landmine for any future caller. **Fix:** decoupled `totalInnings`'s assignment (now unconditional on `tickIndex === 0`, defaulting to `context?.matchLength ?? 9`) from the true-outcome resolution (which still correctly requires `opponentLevelRange` to mean anything) — a match now always progresses and completes even with partial or absent context, falling back to comparing the raw score for its outcome (the same "no such concept" pattern soccer already uses, including the possibility of a genuine `'draw'` in that fallback-only case). Verified directly: a match ticked with zero context, and one with `matchLength` but no `opponentLevelRange`, both now complete normally instead of hanging.
  - **Low severity — a doc comment cited a "simulated calibration" for `MAX_MEANINGFUL_RUN_DIFFERENTIAL=4` that didn't actually exist in CLAUDE.md yet.** Fixed by writing the real number the harness above already measured (~13% blowout rate) directly into the comment, so the claim is backed rather than aspirational.
  - **Low severity — a walk-off check used `===` where its two sibling checks in the same function use `>=`, relying on an unenforced cross-comparison invariant.** Currently safe (an unchanged invariant keeps `inning` from ever exceeding `totalInnings` in the live game), but fragile — a future change to the tied-game nudge (already flagged in its own comment as a simplification) could silently break it with no compiler or test signal. Fixed to `>=` for consistency; changes no current behavior.
  - One candidate (a low-severity, tightly-scoped restatement of the context-freeze finding via a different lens) was folded into the fix above rather than tracked separately, since it described the identical root cause.
  - Re-verified after all fixes: every existing regression harness from prior sessions still passes unchanged, plus a new targeted harness proving both logic fixes directly (a match with no/partial context completes normally instead of freezing; the corrected expected-value formula exactly matches an independent manual calculation at every gap tested, and genuinely differs from the old buggy value at every gap — not always in the same direction, since removing a phantom draw bucket doesn't uniformly raise or lower the number, it just makes it correct).

**Playtested live in the browser, not just simulated.** Unlocked Tee Time from a fresh save, played through at-bats manually (score/outs/inning display updating correctly, "Facing a Level N opponent" and the outcome-independent "Expected payout" preview both working identically to soccer's own established UI conventions), completed a full match (outcome correctly hidden until completion, then revealed via the "Last Result" stat, exactly matching the sixteenth amendment's display-timing convention with zero special-casing needed), hired a manager and confirmed auto-play, and ran a soccer tier and a baseball tier auto-playing SIMULTANEOUSLY at their own independent real-world paces with no interference between them. **Verdict: baseball reads as genuinely distinct from soccer, not a reskin** — the variable match length is directly felt (matches complete after a different number of at-bats each time, unlike soccer's metronomic 90-tick length), the at-bat-by-at-bat pacing has a different rhythm than soccer's continuous minute clock, "Step Up to Bat"/inning-and-outs display reads as its own sport's own vocabulary throughout, and the walk-off rule is an authentic touch soccer has no equivalent of.

**This is Phase 1 of 2 — explicitly a small validation slice, not the full arc.** Per instruction, baseball stops at 3 tiers (real amateur/school levels) and does not yet build the next tiers or a Legacy-style prestige integration. A sensible next phase (a SEPARATE future session, not started here): 2-3 more tiers continuing the same real-then-fictional escalation soccer's own ladder uses — a real "College Baseball"/"Minor League Ball" tier (still 9 real innings, a meaningful jump from Varsity's 7), then breaking into baseball's own grounded→epic→absurd arc (e.g. a "Pro" tier, then progressively larger-than-life ones) mirroring soccer's Sunday-League-to-Multiverse-Cup arc — plus, at that point, a real design decision about whether baseball ever gets its own prestige system or continues sharing soccer's Legacy layer by staying outside its gate.

### Tabbed navigation + per-sport achievement lines (eighteenth amendment to step 2)

Two related changes: replacing the single combined-list screen with three tabs, and extending the achievement framework with per-sport win lines alongside the existing combined one. Touches soccer (step 2), baseball (step 3), and prestige (step 7) simultaneously since the Franchise tab surfaces all three — no build-order step transition, this is UI reorganization + config additions on top of already-shipped systems.

**Block A — Tabbed navigation.** The old single scrolling screen (Legacy panel, Achievements panel, then a combined soccer+baseball venture-tier list — see the "Baseball" amendment) is replaced with three tabs, plain `useState` in `Home.tsx` (`'soccer' | 'baseball' | 'franchise'`), NOT react-router routes — switching tabs is a client-side render swap, never a navigation event.
- **Soccer tab** (`src/components/SoccerTab.tsx`): soccer's own `AchievementsPanel` (filtered to just its own line — see Block C) + the same `revealedTierCount`-sliced soccer venture-tier list as before.
- **Baseball tab** (`src/components/BaseballTab.tsx`): baseball's own filtered `AchievementsPanel` + its 3 venture-tier cards, unchanged from before except no longer interleaved with soccer's.
- **Franchise tab** (`src/components/FranchiseTab.tsx`): `LegacyPanel`, a new "Franchise Stats" section (Total Revenue Earned / Total Matches / Prestige Count — all purely derived by summing `cumulativeRevenue`/`matchesCompleted` across BOTH `tiers` and `baseballTiers`, zero new store fields needed), the combined `totalWins` `AchievementsPanel` line, and a "Developer Tools" section.
- **Total Revenue stays in the persistent app header** (`Home.tsx`), visible across all three tabs — it's one shared currency, not tab-scoped, unlike each tab's own achievement line or tier list.
- **Settings consolidation decision (explicitly asked to pick one and document it):** the old standalone `/settings` route (`src/routes/Settings.tsx`) is REMOVED entirely, not kept as a linked sub-page. Its one piece of content (the "DEV: Wipe All Data" button + confirm dialog) is folded directly into `FranchiseTab.tsx`. Reasoning: with only 3 tabs now, a 4th page reachable via a small link would have reintroduced the exact "main screen → tiny link → separate page" indirection the tabs are replacing; folding it in keeps it exactly as many taps away from the default tab as before (one), with no leftover route. `App.tsx`'s `<Routes>` now has only the `/` → `Home` entry; `react-router-dom` itself is left in place (removing it as a dependency was out of scope for this session).
- **CRITICAL REGRESSION RE-VERIFIED, not assumed:** `useMatchTicker()` still lives in `App.tsx`, called unconditionally above `Home`'s tabs — but the guarantee is now structurally stronger than before, since tabs are local state, not routes, so there is no navigation event of any kind between them for the hook to be affected by. Verified directly in the browser regardless (per this project's own Testing Conventions — "test this directly, don't assume the prior fix generalizes"): hired a manager on The Sunday League (soccer tier 0), switched to the Franchise tab (fully unmounting `SoccerTab`/`SoccerVentureCard`), waited 6 real seconds, and confirmed via the live page that the tier had not just kept ticking but had completed an entire match and started a new one (`matchesCompleted: 0→1`, `lastOutcome: LOSS`, Revenue climbing throughout) the whole time the Soccer tab was out of view.
- CSS: `App.css` gained `.tab-nav`/`.tab-nav__button`/`.tab-nav__button--active`; lost `.app-header__settings-link`/`.settings-page__back-link` (no longer referenced anywhere); `.settings-section*` classes are kept and reused inside `FranchiseTab.tsx` (still exactly as applicable to a section within a tab as they were to a whole page). New `FranchiseTab.css` holds the Franchise Stats grid styling.

**Block B — Win-tracking audit (requested before adding new lines).** Audited whether the existing combined `totalWins` achievement line already correctly counts baseball wins alongside soccer's, since baseball was added in a later session than the achievement framework itself. **Result: it already did, no fix needed.** `useGameStore.ts`'s `tickTier` (soccer) and `tickBaseballTier` (baseball) both increment the exact same `lifetimeStats.totalWins` field from their own completed-match branch, via the same shared `applyEarnedAchievements()` helper — confirmed by reading both call sites directly, not just trusting the doc comments claiming this.

**Block C — Two new achievement lines: "Soccer Wins" and "Baseball Wins."** Added to `src/engine/achievements.ts` as pure config, per the framework's own design (no changes to `checkNewlyEarnedAchievements`, `AchievementReward`, or the reward-granting switch in `useGameStore.ts` — only new `ACHIEVEMENTS` entries + `STAT_LABELS`).
- `LifetimeStats` (`useGameStore.ts`) gained `soccerWins`/`baseballWins` alongside the existing `totalWins` — all three incremented from the exact same single `result.outcome === 'win'` check in `tickTier`/`tickBaseballTier` (never two independent increments that could drift apart), and all three now passed to `applyEarnedAchievements()` symmetrically on both the completion AND non-completion tick branches of both actions (matching the existing established pattern for `totalWins` alone).
- **Schema version bumped 2 → 3** (`CURRENT_SCHEMA_VERSION`), per this project's own standing schema-versioning convention — this is exactly the "shape change nested inside something already persisted" case that convention exists for. `SCHEMA_MIGRATIONS[2]` backfills a version-2 save's missing `soccerWins`/`baseballWins` to exactly **0**, leaving `totalWins` completely untouched. Deliberately NOT retroactively split from existing `totalWins` history — there's no historical per-sport breakdown to recover (this project never tracked which sport a win came from until now), so any nonzero backfill would be fabricated data, not recovered data. An existing player's "Soccer Wins"/"Baseball Wins" badges start accumulating fresh from their very next win in each sport.
- **Threshold calibration — a real simulation, not reused numbers, per instruction not to just copy soccer's onto baseball.** Built a direct-import Node harness (using Node 24's `--experimental-strip-types` plus a small custom ESM resolve hook, since this repo's relative imports have no file extension) driving the REAL `soccerModule.ts`/`baseballModule.ts`/`ventureTiers.ts` code — not a reimplementation — as a discrete-event simulation of a greedy "unlock next tier > hire manager > train" single-sport playthrough (1 manual click/sec on the frontier tier + each tier's own real auto-tick interval once a manager is hired), the same methodology this project's original `totalWins` thresholds used.
  - Soccer (6 starting tiers): reaching 50/250/1000 wins took ~19.2/78.8/302.8 simulated minutes under the CURRENT economy — close enough to the original 15.6/67/260-minute derivation (years of intervening balance changes: milestone multipliers, the margin-bonus formula, etc. account for the drift) that the existing 50/250/1000 numbers were kept for "Soccer Wins," now freshly re-validated rather than merely inherited.
  - Baseball (3 tiers, pace isolated from however long it took to first afford unlocking Tee Time): reaches equivalent WIN COUNTS meaningfully faster in real time than soccer — no draw state (a strictly higher effective win rate) and shorter/variable-inning matches completing in fewer ticks. Copying soccer's 50/250/1000 onto baseball would have made its badges trivially fast to earn by comparison. Instead, baseball's thresholds (**75/300/1200**) were chosen as the win counts that land at roughly the SAME simulated time targets soccer's own 50/250/1000 hit (~19/79/303 min): the simulation's data points (interpolated) put 75 wins at ~17.7 min, 300 at ~71.9 min, and 1200 at ~272.4 min.
  - Rewards: each new line's Bronze/Silver/Gold pays roughly HALF of the combined `totalWins` line's equivalent tier (+100/+1000 Revenue, +5 Legacy Points vs. the combined line's +200/+2000/+10) — a deliberate, documented choice so the two new lines read as a fun sport-specific complement, not a second copy of the "biggest" combined milestone's reward.
- **Display:** `AchievementsPanel.tsx` gained an optional `statKeys?: StatKey[]` prop filtering which achievement line groups render (omit to show all — the original behavior, preserved as a default). `SoccerTab`/`BaseballTab`/`FranchiseTab` each pass their own single-element array (`['soccerWins']`/`['baseballWins']`/`['totalWins']`).

**Adversarial review and fixes.** A workflow-based review (3 lenses × adversarial verification, 7 agents) specifically hunting for bugs in this session's diff raised 4 candidate findings; all 4 survived verification and were fixed:
- **High severity — a save already AT the current schema version with a corrupted `lifetimeStats` (e.g. hand-edited to `null`) crashed the whole app on the very first render.** `merge()` already shape-guarded `tiers`/`baseballTiers`/`notifications` but had no equivalent guard for `lifetimeStats` — and since zustand's `migrate()` only runs on a version MISMATCH, a corrupted-but-current-version save skips `SCHEMA_MIGRATIONS[2]`'s own backfill entirely and sails straight through the old blind `{...currentState, ...persistedState}` spread. `AchievementsPanel.tsx` (always mounted on the default Soccer tab) unconditionally reads `s.lifetimeStats.totalWins`/`.soccerWins`/`.baseballWins` on first render, which threw a `TypeError` on `null` — the exact "never trust persisted shape" gap this project already hardened `tiers`/`notifications` against once before (fifteenth amendment), just not yet extended to this new field.
- **Medium severity, same root cause — a `lifetimeStats` present but missing `soccerWins`/`baseballWins` (also already at the current schema version) silently poisoned both counters to `NaN` on the very next win in that sport,** since `tickTier`/`tickBaseballTier`'s completion branch computes `s.lifetimeStats.soccerWins + (isWin ? 1 : 0)` with no fallback — `undefined + 1` is `NaN`, and `NaN >= threshold` is always `false`, permanently and silently blocking that save's soccer-wins-/baseball-wins-achievement tiers with no visible symptom.
  - **Fix for both:** a single shared `sanitizeLifetimeStats(value: unknown): LifetimeStats` helper (`useGameStore.ts`) — backfills any missing/non-finite field to 0, using `Number.isFinite` rather than `typeof x === 'number'` specifically because `typeof NaN === 'number'` is true and would have let an already-poisoned NaN through as "valid." Used by BOTH `SCHEMA_MIGRATIONS[2]` (the version-gated path) AND a new guard line in `merge()` (the always-runs path) — the same "one authoritative source" pattern this file already uses for `tiers`/`baseballTiers`, so the two call sites can't drift apart. Verified with two new targeted harnesses (one seeding a `lifetimeStats: null` save already at schema version 3, one seeding `{totalWins: 42}` missing the two new fields, both bypassing `migrate()` by construction): the first no longer crashes and sanitizes to a fresh `{0,0,0}` with the rest of the save intact; the second backfills to `{totalWins: 42, soccerWins: 0, baseballWins: 0}` and a subsequent real forced win via the actual `tickTier` action correctly yields `soccerWins: 1` (not `NaN`).
- **Low severity — `AchievementConfig.statTracked` and `AchievementsPanel`'s `statKeys` prop were both plain `string`/`string[]`, so a typo in either (a new achievement line's `statTracked`, or a tab's `statKeys` filter array) would silently render an empty Achievements panel** (just the header, zero lines/badges) with no error — indistinguishable from "nothing earned yet." **Fix:** `STAT_LABELS` (`achievements.ts`) is now declared `as const`, and a new exported `StatKey = keyof typeof STAT_LABELS` type is used for both `AchievementConfig.statTracked` and `AchievementsPanelProps.statKeys`. This doesn't fight the framework's own "keep `statTracked` open, not an enum" design intent — `STAT_LABELS` already required a new entry per new achievement line before this change; deriving a type from it costs nothing extra while catching a cross-file mismatch at compile time instead of silently at render time. `tier` (Bronze/Silver/Gold/Rookie/...) deliberately stays a plain `string`, unaffected — a new tier name has nothing else that needs to agree with it, unlike a stat key.
- **Low severity — this file's own twelfth-amendment "Settings page" section still described `/settings`, `src/routes/Settings.tsx`, and `.app-header__settings-link` as live architecture,** none of which exist anymore after Block A above, and — unlike every other superseded decision in this file — it carried no strikethrough or pointer to what replaced it. Fixed: struck through in place with a pointer to this section, matching this file's own established convention.

**Verified, per this project's own Testing Conventions (schema/persistence + achievement-crediting logic is squarely the high-risk case; the tab restructuring itself is presentational/routing).**
- Build/lint clean, before AND after the adversarial-review fixes above.
- Live browser check: all 3 tabs render their correct, distinct content (confirmed via the actual rendered text, not just code reading); the regression check described in Block A.
- A direct-import Node harness (localStorage polyfilled, real `useGameStore.ts` imported unmodified) confirmed: a version-2 save with `lifetimeStats: {totalWins: 77}` (no soccerWins/baseballWins keys at all) migrates to `{totalWins: 77, soccerWins: 0, baseballWins: 0}` with every other field (Revenue, Legacy Points, earned achievement ids) untouched; a fresh store and a post-progress `resetProgress()` both yield `{0, 0, 0}`; forcing (via a temporarily mocked `Math.random`) exactly 50 real soccer wins through the real `tickTier` action yields `soccerWins: 50, baseballWins: 0, totalWins: 50` and grants exactly `soccer-wins-bronze` + `total-wins-bronze` (and nothing else); forcing 75 more real baseball wins through `tickBaseballTier` afterward leaves `soccerWins` untouched at 50, sets `baseballWins: 75`, `totalWins: 125`, and grants exactly `baseball-wins-bronze`.
- Two additional targeted harnesses (written specifically to prove the two schema/persistence fixes above, both bypassing `migrate()` by seeding a save already at the current schema version) confirmed the null-crash and NaN-poisoning bugs are fixed, as detailed in "Adversarial review and fixes" above.

## Build Order (current status — update the checkboxes as work completes)
- [x] 1. Scaffold React/Vite + Zustand, basic layout, empty store
- [x] 2. Single-sport match-sim tick loop, one currency, minimal UI
- [x] 3. Second sport as a plugged-in module (validates engine abstraction) — Phase 1: baseball, 3-tier validation slice (Tee Time/Little League Nights/Varsity Diamond). Full arc expansion is separate future work.
- [ ] 4. Player development, scouting, contracts
- [ ] 5. Facilities/upgrades system
- [ ] 6. Season structure, standings, promotion/relegation
- [x] 7. Prestige / Franchise Legacy reset system
- [ ] 8. Invitational Games event system
- [ ] 9. UI polish + integrate final art assets
- [ ] 10. Capacitor wrap for iOS

## Out of Scope for v1
- Multiplayer / leaderboards
- More than 2 sports
- Backend/server
- Monetization/IAP implementation (architecture should allow it; don't build it)
- Rich illustrated art (2D icons only)

## Conventions
- Scoped sessions: work one build-order step at a time. Stop and summarize before starting the next step unless explicitly told to continue.
- Prefer clarity/maintainability over premature optimization — this is a solo long-term project, not a performance-critical app.
- Solo git workflow: commit directly to `main` for all build-order steps. Do not create feature branches or open pull requests unless explicitly asked.
- End-of-session deliverables: every session ends with (1) a written summary of what was done, (2) a plain-text description of the current UI state as observed via the browser preview tool (what's rendered, what changed visually) — even without a screenshot, so the user can relay it to another reviewer — and (3) a playable link left running (start/restart the dev server via the browser preview tool, clear `localStorage` for a fresh-player state, then give the `http://localhost:5173` URL — noting it's local-only and stops when the session ends). Don't stop the server as a "cleanup" step; leave it running and reset instead.

## Testing Conventions
Verification depth should scale with the risk of the area being touched, not be applied uniformly — this is a standing rule for how future sessions should calibrate their own effort, not something that needs to be re-specified in every prompt.

- **Cosmetic/presentational-only changes** (styling, copy, renames, layout, icon swaps) — build + lint + a basic browser confirmation that the change renders as intended is enough. Don't reach for harnesses or adversarial review for a color swap.
- **Changes touching store logic, economy/payout calculations, match-resolution, prestige, or save/schema/persistence** — these get the full adversarial/dual-verification treatment: a direct-import Node harness exercising the real functions (not a reimplementation) covering the normal path AND edge cases (fresh state, mid-transition, boundary/saturation cases, legacy/migrated save shapes), plus an adversarial review pass (a workflow, or at minimum an independent agent/self-review explicitly hunting for ways the change could be wrong) before calling it done.
- **Why these specific areas:** this project's own history is the evidence, not a hypothetical — schema/migration bugs (ninth, fourteenth, fifteenth amendments), a hidden-tier-reveal exploit via hand-edited flags (ninth), a getPerformanceFactor bonus-leak that silently overpaid a downgraded outcome (thirteenth), the SAME class of leak reopened by an unrelated cosmetic change two sessions later (fifteenth), and a notification-crash/non-array-tiers-crash pair introduced by a well-intentioned refactor (fourteenth, fifteenth) — every one of these was in exactly the areas called out above, and every one shipped once before an adversarial pass caught it. Cosmetic-only sessions in this same history (sixth, tenth amendments) have never produced an equivalent incident.
- **When a single session mixes risk levels** (e.g. one cosmetic change alongside one store-logic change), calibrate per change, not for the session as a whole — a low-risk change bundled alongside a high-risk one doesn't need the heavy treatment just because they shipped together, and a high-risk change doesn't get to skip it just because most of the session was cosmetic.
- **Full multi-agent Workflow-tool review is not the only way to satisfy the "high-risk" bar** — it's the deepest option, appropriate when the user has opted into multi-agent orchestration or the change is large/tangled enough to warrant it. A direct-import Node harness plus a single independent adversarial review pass (self-review or one agent explicitly trying to refute the implementation) is a legitimate, lighter-weight way to satisfy the same "don't skip verification on high-risk areas" principle when a full workflow isn't warranted or requested.

## Maintenance
After any session that completes a meaningful chunk of work (a full build-order step, a significant architecture decision, or a notable deviation from the plan above), **regenerate this file**: update the checkboxes, add any new conventions or architecture decisions actually made, and note deviations from the original brief. Keep it concise — this file should always reflect current reality, not the full history of how it got there.
